## Текущая задача
Доступ Веры: второе пространство «Менеджер» (полный MGlass B2C + B2B) поверх её логист/закупщик контура, с переключателем вкладок. Сделано, в проде.

## Что сделано (эта сессия)
- КП: цена одной суммой (скрыты построчные цены услуг), переключатель в панели — PR #63, прод
- Доступ Веры (PR #64, прод, 6d53d85):
  - lib/permissions.ts → флаг manager_workspace
  - lib/getRole.ts → canAccessRoute/explainAccess: buyer+managerWorkspace открывает ROLE_ALLOWED.manager
  - middleware.ts → флаг из permissions, кука user-mgr-ws (bound userId), в гейт
  - components/Sidebar.tsx → renderManagerWorkspace()/renderBuyerLogist() + вкладки [Менеджер|Логист] для buyer с флагом
  - app/admin/users/page.tsx → тумблер «+ Менеджер» у закупщиков
  - accessMatrix.test.ts → покрытие (не течёт в др. роли/финконтур)
  - БД: Вере (zakupka@mglass.ru) manager_workspace:true + can_view_all_deals=true

## Следующий шаг
Ждать следующую задачу. Вере попросить перезайти/обновить — увидит вкладки [Менеджер | Логист], полный доступ.

## Контекст
- Роль Веры осталась buyer (её логист-контур не тронут); менеджер добавлен поверх флагом — безопаснее смены роли.
- Переключатель вкладок для не-admin теперь возможен: buyer+manager_workspace. Тот же паттерн можно расширить на другие роли при надобности.
- CI (ci.yml, on: pull_request) иногда не стартует сразу на новом PR — помогает close/reopen; локально гоняю полный lint+test как эквивалент.

## Открытые вопросы
Визуально сайдбар Веры не проверить без её логина (её сессия). Функционально подтверждено: БД, тесты 149/149, прод READY.
