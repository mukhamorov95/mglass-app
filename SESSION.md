## Текущая задача
Голосовая диктовка починена: OpenAI-квота исчерпана → добавлен фолбэк Groq. Ключ в проде, деплой READY. Ждём финальный «живой» тест микрофоном от владельца.

## Что сделано (эта сессия)
- КП: цена одной суммой (PR #63)
- Доступ Веры: пространство «Менеджер» + вкладки (PR #64)
- Голос (PR #65): lib/transcribe.ts (OpenAI→Groq фолбэк + понятные ошибки),
  подключён в installations/parse-voice, accounting/notes, vlad/notes, ai/kp-transcribe
- Владелец добавил GROQ_API_KEY в Vercel (Production); прод передеплоен (READY).
  Groq проверен прямым вызовом (HTTP 200, точная расшифровка ru). Claude-разбор HTTP 200.

## Следующий шаг
Владельцу сделать реальный тест: /installations → «＋ Заявка на монтаж» → 🎙 Надиктовать
→ проверить, что поля заполнились. Если ок — задача закрыта.

## Контекст
- Фактический корень был не в коде, а в биллинге OpenAI (Whisper 429 insufficient_quota).
- Теперь primary OpenAI, при 429/402 автопадение на Groq (whisper-large-v3-turbo).
  Как пополнят OpenAI — снова станет основным сам.
- ⚠️ Groq-ключ владелец прислал в чат — рекомендовал отозвать и создать новый.
- Не переведены на модуль (прямой OpenAI): amo/calls/transcribe, amo/calls/analyze
  (фоновые разборы звонков) — при желании тоже на transcribeRu.

## Открытые вопросы
Финальный тест микрофоном — за владельцем (mic-доступ из сессии не эмулировать).
