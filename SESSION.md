## Текущая задача
КП: скрыть построчные цены услуг — показывать клиенту только итог одной суммой. Сделано и в проде.

## Что сделано (эта сессия)
- components/QuotePDF.tsx (серверный PDF /api/quotes/[id]/pdf) → колонка «Сумма» и построчные цены услуг под флагом showPrices; ширины колонок перераспределены (без пустоты справа)
- app/b2b-quotes/[id]/kp/page.tsx (экранный КП + Скачать PDF) → тот же showPrices + селектор «Цена одной суммой / Цены по позициям» в панели
- app/api/quotes/[id]/pdf/route.ts → priceMode из notes.kp_price_mode в пропсы
- __tests__/kp/quotePdf.render.test.ts → рендер PDF в обоих режимах отдаёт валидный %PDF-, дефолт = consolidated
- Режим хранится в b2b_orders.notes.kp_price_mode, читается обоими рендерерами; дефолт (пусто) = consolidated
- PR #63 смержен (4d5fd34), Vercel prod READY (mglass-app.vercel.app)

## Следующий шаг
Ждать следующую задачу владельца. Экранный КП проверен на проде (заказ 5033 LoLegko): колонки «Стоимость» нет, услуги без цен, только ИТОГО 88 553 ₽.

## Контекст
- Два рендерера КП: QuotePDF.tsx (серверный, attachment-скачивание, имя файла КП-<id>-<Клиент>.pdf) и b2b-quotes/[id]/kp/page.tsx (экранный html2canvas). Оба теперь на одной логике priceMode.
- AI-КП (commercial_proposals → app/kp/[id]/print) — отдельный контур, НЕ трогали (там менеджер сам диктует построчные суммы).
- CI (ci.yml, on: pull_request) один раз не выстрелил на PR #63 — помог close/reopen. Локально гонял полный lint (0 ошибок) + test (144/144).

## Открытые вопросы
Нет. Если понадобится где-то показать построчную цену (B2B-листы) — переключатель «Цены по позициям» в панели КП.
