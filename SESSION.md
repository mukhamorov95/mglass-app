## Текущая задача
Починен баг: фото/чертёж не вставлялся на 3-й лист КП (генератор /kp). Выкачено PR #106.

## Что сделано (эта сессия)
- Диагностика: 3 обрыва в цепочке «вставить фото/чертёж → лист 3 КП»
- app/api/kp/photo/route.ts (новый) → заливка фото сервисным ключом (обходит storage-RLS bucket kp-photos, где не было INSERT-политики)
- app/kp/page.tsx → uploadPhoto через роут + pdfFirstPageToPng (PDF→PNG 1-й стр., pdf.js) + accept image/*,application/pdf + видимая ошибка
- app/kp/[id]/print/KpDocument.tsx → crossOrigin="anonymous" на фото листа 3
- app/kp/[id]/print/page.tsx → waitImages (ждём все картинки), catch на ошибку, ?print=1 ждёт фото
- supabase/migrations/20260731_kp_photos_storage_policies.sql (необязательная — роут не зависит)
- build ✓, lint 0 ошибок, PR #106 squash-merge в main

## Следующий шаг
Владелец проверяет в проде: /kp → приложить фото ИЛИ PDF-чертёж → сохранить → Скачать PDF → фото на 3-м листе.
Далее по приоритету: раздел «B2B-партнёры» (дашборд+фильтр→кабинет) ИЛИ Ф4-остаток кабинета партнёра (документы/дебиторка/повтор) ИЛИ бонусный фонд цеха (#178).

## Контекст
Партнёрский кабинет MR GLASS собран Ф0→Ф4-каталог (PR #103–105). Раздел «B2B-партнёры» (наш внутренний) ещё не построен.
Supabase MCP отключён — SQL напрямую не выполнить; проверка через UI/владельца в проде.

## Открытые вопросы
- Заливка фото/PDF и вставка в PDF на листе 3 — финальная визуальная проверка в проде за владельцем.
