## Текущая задача
Маршрут менеджерского контура. **B2B (А1–А20) пройден полностью и в проде.**
B2C: сделаны М1 (единая карточка сделки по нормализованному телефону, #267) и
М2 (расчёт из карточки уносит клиента, #268). Дальше — решение владельца: идти
по исходному списку М3–М20 или перекроить его под живые данные (см. вопросы).

## Что сделано (эта сессия)
- Ручная корректировка итога просчёта + предупреждение о доходе → PR #222
- Маршрут А0→А20 / М1→М20 → docs/MANAGER_CONTOUR_ROADMAP.md (#241)
- А1 карточка клиента менеджеру, А3 повтор и шаблоны, А7 УПД → #242
- А2 ссылка на КП клиенту, А5 согласование по ссылке → #245
- Утечка себестоимости в кабинет партнёра закрыта (белый список) → #249
- А6 единый расчёт срока, А8 ссылка на оплату → #246
- А9 дебиторка, А10 реестр счетов /b2b-invoices → #247
- А11 согласование цены с тонкой маржой → #250
- А13 остатки склада в просчёте → #253
- А14 уведомления менеджеру в Telegram, А15 /b2b-today → #252
- А12 индивидуальный прайс клиента (миграция 20260828) → #257
- А16 логистика и упаковочный лист → #258
- А17 рекламации и себестоимость переделки → #259
- А18 план/факт менеджера (миграция 20260829) → #260
- А19 файл клиента → позиции просчёта → #261
- А20 лента действий клиента + мгновенное уведомление об отгрузке → #263
- А4 одна карточка сделки /b2b-deal/[id] → #264

## Следующий шаг
М1 — единая карточка сделки B2C (по образцу /b2b-deal): заявка → замер → расчёт →
КП → договор → оплата → производство → монтаж → акт. Дальше М2–М20 по маршруту.

## Контекст
Каждый шаг = отдельная ветка и PR, мерж в main после зелёных tsc/eslint/vitest (466 тестов).
Новые общие модули: lib/b2b/priceOverride, deadline, publicQuote, clientPrices, clientTimeline, notifyManager.
Публичное КП: /p/kp/<token>, whitelist полей, тест на утечку себестоимости.

## Открытые вопросы
- ГЛАВНОЕ ПО B2C: контур в приложении почти не используется. Живое: crm_leads 285
  (все за 3 мес), calculations 91 (35), contracts 26 (26). Мёртвое: orders (B2C) 1,
  installations 3, measure_requests 2, measurements 0. Для сравнения b2b_orders 5073.
  Шаги М12–М16 (график монтажа, мобильный замерщик, акты, гарантия) строятся поверх
  пустых таблиц. Предложение — перекроить остаток М-блока под живую часть:
  заявка → расчёт → КП → договор → оплата.
- Расчёты не привязаны к клиенту: телефон заполнен у 7 из 91, amo_lead_id — ни у одного.
- ВЛАДЕЛЬЦУ ПРИМЕНИТЬ МИГРАЦИИ: 20260828_b2b_client_prices.sql, 20260829_b2b_manager_plans.sql.
  До этого прайс клиента пустой (считаем по общему), план менеджера не сохраняется.
- А8: онлайн-оплата ждёт выбора эквайринга и ключей (PAYMENT_PROVIDER) — сейчас честный 501.
- Слияние списков просчётов и заказов в один (полное А4) — отдельное решение владельца.
