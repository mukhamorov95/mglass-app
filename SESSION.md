## Текущая задача
Вкладка «Видео-обучение». Мерилка обучения в проде; разбор чертежа проверен — сломанного
звена нет, зато найден и починен свой промах со знаком диаметра. Ролик не снимаем.

## Что сделано (эта сессия)
- `/admin/quote-quality` + `quote_quality_weekly(date)` — базовая линия обучения (#385)
- `scripts/narrate.mjs`: длительность считалась как для стерео, `say` пишет моно —
  слайд держался половину времени. Теперь `afinfo`; озвучка 109 с, а не 55 с (#385)
- Очередь цеха: «размеры не указаны» больше не печатается над комментарием,
  где диаметры уже есть → «отверстия — размеры в комментарии» (#388)
- «Упаковано сегодня» / «Отгружено сегодня» + копирование текста в буфер на
  `/production-app/shipping`; автоотправки нет сознательно (#388)
- **Знак диаметра в базе двух видов:** Ø (U+00D8) — 35 позиций, ∅ (U+2205) — 30.
  Детектор ловил только Ø и терял 46%. Расширен до `[ØøΦφ⌀∅]` в `holesFromComment()`
  и в функции метрики; миграция применена, пересчёт на проде 65 вместо 35 (#395)

## Следующий шаг
Ждать решения оркестратора: учить надо не «заполните группы», а «прогоните чертёж
клиента через разбор» — кнопкой не пользуются Вера, Дмитрий и Яна (0 разборов).

## Контекст
`notes.drawing_url` (`order-drawings/<id>.pdf`) — рабочий чертёж ДЛЯ цеха, а не вход
разбора. Вход — файл клиента в `/api/b2b/parse-pdf`, он нигде не сохраняется.
Связи между ними нет и не было: «49 чертежей Веры → 0 диаметров» не поломка.
Логов разбора не существует (`parse-pdf` пишет только auth), проверять было нечем.
Разбор используют трое: Администратор 40 позиций, Нуржан 27, Айжан 10; Вера 0.
`notes.stages.shipped` в трёх видах: 3788 `true`, 404 ISO, 187 date-only → `stageDayKey()`.

## Открытые вопросы
- Ссылка на `/admin/quote-quality` в Sidebar — за оркестратором
- `content/video/calc-whats-new.json` (не `prochet-a-ya.json`): в s01 «Коротко: три минуты»
  при 109 с, в s03 имена вместо станций. Снимается он или нет — решает оркестратор
