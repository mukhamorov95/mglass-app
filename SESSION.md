## Текущая задача
Вкладка «Прайс душевых». По маршруту docs/pricing/ROADMAP.md пройдены П1–П5 + A40.
Жду приёмку PR #292 (П5) у оркестратора и следующий шаг.

## Что сделано (эта сессия, всё в main кроме #292)
- Движок прайса по моделям, аудит (П1), маржа из financial_settings (П2)
- П3 история цен + сверка с прайсом; A40 «где переплачиваем» (#286)
- П4 версии прайса + срок КП (#290, смержен)
- П5 монтаж/логистика по зонам (#292, на приёмке): зоны доставки (база+₽/км+лимит),
  надбавки монтажа (за секцию/разово), разбивка installBase/installExtra/deliveryZone

## Следующий шаг
По назначению оркестратора: либо реальные данные зон заводит владелец, либо П7 (общий
раскрой на заказ — чистый), либо П6 (закупочный лист) ТОЛЬКО через контракт со складом
(потребляю reserveForOrder→shortages, свою закупку не строю).

## Контекст
Оркестратор = backbone-сессия mglass-app-83 (реестр docs/ORCHESTRATION.md). Цикл:
задача с критериями → PR → приёмка → мерж. Приоритеты владельца оркестратор не переставляет.
Границы: мой контур configurator_* + supplier-catalog; b2b-просчёты, b2b_materials, склад — не мои.
Формат KitLine→order_components согласован с client-cabinet: unitPrice=СЕБЕСТОИМОСТЬ→unit_cost,
role шлю как есть (словарь role→kind у них). Нюанс: для bar-ролей total точный, unit_cost×qty
даёт дрейф округления — для bar слать qty=1/unit_cost=total (решить при сборке корзины в П6).

## Открытые вопросы владельцу
- Реальные зоны доставки и надбавки монтажа (суммы) — сейчас пусто, работает как Москва-фикс.
- Ролики, ручка-купе, штанга/крепление 45°, стабилизатор, премиум-тариф — не заведены (аудит: 1 из 9 моделей продаваема).
- «Заглушка сверху» — над проёмом двери или весь верхний профиль.
