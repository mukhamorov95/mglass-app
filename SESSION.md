## Текущая задача
Вкладка «Видео-обучение». Мерилка полноты просчёта в проде (#385). Сделаны две правки
для цеха: диаметры из чертежа больше не прячутся за «размеры не указаны», и вечерний
отчёт в Telegram собирается кнопкой вместо ручного набора.

## Что сделано (эта сессия)
- `/admin/quote-quality` + `quote_quality_weekly(date)` (миграция ПРИМЕНЕНА, проверено
  по `pg_proc`: `prosecdef=false`, acl `{postgres, service_role}`) — базовая линия обучения
- `scripts/narrate.mjs`: длительность считалась как для стерео, `say` пишет моно —
  слайд держался половину времени. Теперь `afinfo`. Озвучка ролика 109 с, а не 55 с
- `lib/production/holes.ts` → `holesFromComment()`: в `my-queue` строка «отверстия
  (размеры не указаны)» печаталась НАД комментарием, где диаметры уже есть (35 позиций
  из 83). Теперь при наличии ⌀ пишем «отверстия — размеры в комментарии»
- `lib/production/dayLists.ts` + `app/production-app/shipping/DayReport.tsx`:
  «Упаковано сегодня» и «Отгружено сегодня» списком на экране отгрузки + копирование
  текста в буфер (номера в столбик, как Никита пишет руками). Автоотправки нет
- 29 новых тестов (16 dayLists + 6 holesFromComment + ранее 9 quoteQuality); 835/835 зелёные

## Следующий шаг
Ждать ответа оркестратора по двум находкам ниже. Ролик не снимаем до его правок сценария.

## Контекст
`notes.stages.shipped` живёт в ТРЁХ видах одновременно: 3788 голых `true`, 404 полных ISO
(пишет `api/production/ship`, сегодня тоже), 187 date-only (пишет `app/b2b-orders/page.tsx`).
`packaged` — 496 ISO + 227 date-only. Поэтому день считается через `stageDayKey()`:
`true` → null, date-only как есть, ISO → `mskDayKey`.
Частичной отгрузки в данных НЕТ: `shipped` — отметка на заказ целиком. Хвост
«( осталось N шт)» считается только для упаковки, из незакрытых задач упаковки.

## Открытые вопросы
- Реестр #274 «date-only в обоих писателях» устарел: `api/production/ship` пишет полный ISO.
  Это backbone-файл оркестратора — отдано строкой, сама не трогаю
- В карточке заказа (`orders/[id]`) отверстий нет в блоке признаков вовсе, а комментарий
  с диаметрами показан 10 px серым курсивом. Это экран, где жмут «Готово»
- Ссылка на `/admin/quote-quality` в Sidebar — за оркестратором
