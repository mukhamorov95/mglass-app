## Текущая задача
Владелец уехал на ~4 часа. Задание: разобрать систему целиком + отдельно маркетинг (он не проработан). Цель — кратный поток лидов B2B и B2C. Результат: что хорошо / что улучшить / что добавить, с пользой здесь и сейчас.

## Что сделано (эта сессия)
- Монтаж из заказа/КП → предзаполненная форма `/installations`; монтажи видны в `/calendar` (PR #67)
- Мост «счёт → деньги → Отдел продаж»: `crm_sales.contract_id`, `upsertSaleFromContract`, `/api/contracts/[id]/payment`, карточка поступлений (PR #68)
- Отдел продаж только M-Glass (`voided=false AND department≠'b2b'`) + «поступило» отдельно от «продано». B2B занижал средний чек: в апреле 55 B2B-строк против 37 розничных
- Фикс индекса: `ON CONFLICT` не подхватывает частичный индекс → обычный уникальный (PR #69)
- Единый ярлык материала `lib/materialLabel.ts` — цех не мог отличить зеркало от стекла («Осветлённое» есть и то, и другое). Подключено в 8 мест, включая КП и наклейки (PR #70)
- Изделия производства теряли габариты (нули в коде) → `lib/b2bFactoryProducts.ts`; бэкфилл 15 позиций в 8 заказах
- Проблемы цеха: причина обязательна на обоих экранах, `/api/b2b-orders/[id]/problem`, экран `/production-app/problems` (канбан + сумма), миграция автора (PR #71)
- Дубль деталей ДОДЕЛАН: карточка = деталь, внутри этапы (сначала был поправлен только счётчик — владелец справедливо указал)
- Розница: оплата заказа пишет в payments + crm_sales (шаг Д2) (PR #72)
- Аудит цеха: `production_lead` не существовал в БД (миграция не применена) → **Дмитрий месяц не мог отметить ни одного этапа**, флаг выставлен; заказ 0928-3 висел 16 дней без задач — починен; выручка на `/production-app/voronezh` была видна всем — закрыта `useCanViewMoney` (PR #73)
- **RLS волна 1** (PR #74): закрыты 9 серверных таблиц, включая `telegram_auth_codes` (одноразовые КОДЫ ВХОДА читались публичным ключом). Проверено эмпирически: закрытые → `[]`, незакрытые прайсы всё ещё отдают 543/32/60 строк

## Следующий шаг
Дождаться 4 разборов (маркетинг-контур, воронка/данные, рынок РФ, система целиком) → собрать отчёт владельцу.
Потом по плану: RLS волна 2 (см. мины ниже), затем задачи #159–#162.

## Контекст
**Мины перед RLS волной 2 (обязательно к прочтению):**
1. `glass_price_matrix` — политика уже есть, но `FOR ALL`. Простое включение RLS **выдаст право записи в прайс каждому авторизованному, включая мастеров цеха**. Сузить до SELECT ДО включения.
2. Калькуляторы душевой и лофта (`app/calculator/shower/page.tsx:5`, `loft/page.tsx:5`) используют устаревший `lib/supabase.ts` — вероятно ходят как `anon`, а не `authenticated`. Все политики проекта написаны `TO authenticated` → эти экраны получат пустой прайс. Перевести на `@/lib/supabase-browser` ДО закрытия прайсов.

**Обратная дыра (RLS вкл, политик 0 → браузер не видит ничего):** `activity_log` (журнал действий ПУСТ, 0 строк — записи глушатся try/catch), `suppliers` (17 строк), `b2b_material_sheet_variants` (81), `b2b_client_manager_history` (40), `appointments`, `brigades`, `calculation_changes`, `call_analyses`.

**Спящие B2B-клиенты (проверено):** Глеб — 16 заказов, 316 316 ₽, молчит 127 дней. ООО МОСПРОМЗНАК — 19 заказов, 202 133 ₽, молчит 75 дней. Остальные разовые.

**Известное, но не сделанное:** гонка на `notes` теряет 11% отметок (110 в 46 заказах); авторизация цеха только в UI; агрегаты CEO завышены ~37% (нет фильтра PROD_SINCE); 35 заказов застряли в «готово» — в цехе нет шага отгрузки.

## Открытые вопросы
- Валерия и Сергей без станций — видят пустую очередь. Так задумано или забыли?
- Три надиктованных сообщения (6000 К; зеркало Crystal Vision; ответ Татьяне) — ушли не в то окно, действий не предпринято
