## Текущая задача
B2B-доработки калькулятора выполнены (5a+5b смержены). Жду следующую задачу владельца. Configurator-ветка — параллельно, ниже.

## Что сделано (эта сессия)
- ✅ **Фактурное стекло (5b) — PR #175 в main.** /admin/glass-prices (модалка форматов) — селектор направления рисунка материала (Нет / вдоль длины / вдоль ширины) → b2b_materials.pattern_direction через PATCH /api/admin/b2b-materials/pattern (requireAdmin). Путь себестоимости (orderEconomics→materialUsage→cuttingOptimizer) уже уважал флаг (respect_pattern→canRotate:false). Колонка pattern_direction (text) в БД есть.
- ✅ **B2B-калькулятор (5a) — PR #177 в main.** (1) Разбор чертежа: если материал на чертеже явно не назван — считаем ВЫБРАННЫМ в форме материалом/толщиной/типом (был баг: matchDrawingMaterial при hint без ключевых слов, напр. «Триада CH», всегда возвращал М1). (2) Мультивыбор позиций (чекбоксы + выбрать-все с indeterminate) + панель массовых действий: сменить материал (толщина→тип, пересчёт через recomputeWithMaterial с сохранением геометрии/услуг/флагов/договорной цены) или удалить выбранные. (3) Подсветка фактурного стекла в раскрое: бейдж «рисунок вдоль длины/ширины». Файл: app/calculator/b2b/page.tsx.

## Следующий шаг
Ждать задачу владельца ИЛИ вернуться к 3D-конфигуратору (ниже). Возможная доводка 5a при фидбэке: массовая смена толщины/типа отдельными контролами (сейчас — единый пикер материал-строки, который меняет и то, и другое); визуальная проверка мультивыбора в браузере (страница за авторизацией — нужен логин).

---

## Параллельная ветка: 3D-конфигуратор — ЦЕНООБРАЗОВАНИЕ (Шаги 1–5) ЗАДЕПЛОЕНО
Все 5 шагов ценообразования закрыты и в проде (main 38918c5):
- Шаг 1–2: computeQuantities (cut-list в мм: секции, стекло м², профиль/штанга хлысты, фурнитура шт) + computePrice по канону «Быстрого расчёта» (Цена=Себест/(1−маржа−налог)), монтаж 6500/секц + доставка 5000 сверху без наценки. Хлысты 2200/3000: кусок ≤2200→2200, >2200→3000; себест = сумма цен хлыстов.
- Шаг 3: админ-сервис /admin/visualizer-pricing (Бюджет/Премиум, клик модель → 3D+спека+себест, ручной ввод цен фурнитуры по цвету, стекло по типу, хлысты, монтаж/доставка) → Supabase configurator_pricing (service-role, RLS без публичных политик).
- Шаг 4 (ГОТОВО, задеплоено): живой провод админка→клиент. /api/configurator/quote (публичный, POST) читает цены из Supabase, считает НА СЕРВЕРЕ, себестоимость НЕ уходит в браузер: неавторизованному — только «от N ₽», авторизованному менеджеру — полная разбивка. Клиент фетчит цену с debounce 250ms; спека мгновенная. middleware: /api/configurator/ публичный.
- Шаг 5: в панели менеджера себест разбита на 2 строки (стекло / фурнитура). Тесты 26/26.

## Следующий шаг конфигуратора
Владелец забивает реальные цены в /admin/visualizer-pricing (Бюджет+Премиум) → они сразу в клиентском расчёте. Дальше по дорожной карте: выноски с фото фурнитуры, экспорт эскиза (PDF), лид→CRM с Tilda, голый embed-layout, доп. модели.

## (архив) 3D-конфигуратор — базовый визуал
3D-конфигуратор душевых для сайта (встраиваемый виджет). Изолированная ветка feat/shower-3d-configurator от чистого main. Клиент выбирает тип → размеры → стекло/финиш/петля → видит НАСТОЯЩУЮ 3D-модель (WebGL), которая масштабируется вживую. Референс UX — privetmaket.ru/shkaf.

## Что сделано (эта сессия)
- Изоляция: ветка feat/shower-3d-configurator от origin/main (5dff89b). Чужой WIP на feat/railing-calculator спрятан в stash (restore: `git checkout feat/railing-calculator && git stash pop`).
- Установлен 3D-стек: three + @react-three/fiber + @react-three/drei + @types/three (раньше 3D-библиотек в проекте НЕ было).
- Диагноз «начатого»: /configurator уже имеет отличный ПАРАМЕТРИЧЕСКИЙ слой (lib/configurator/catalog.ts — 8 типов, panels(d)→Panel[], bom(d), реальные артикулы петель/профилей/штанг, финиши). Визуал же был плоский изо-SVG (PartitionIso.tsx) — вот что «неправильно». Каталог ПЕРЕИСПОЛЬЗОВАН, добавлен настоящий 3D-рендер поверх него.
- components/configurator/scene/assembly.ts (новый) → чистый билдер Configuration→примитивы (мм×0.001 = метры), топология зеркалит изо: фронт вдоль X, глубина вдоль Z, угловые складываются под 90°, распашная дверь приоткрыта поворотом вокруг петлевой кромки, раздвижная вынесена по Z, петли+ручка на двери. Тест __tests__/configurator/assembly.test.ts — 5/5 зелёные.
- components/configurator/Partition3D.tsx (новый) → R3F Canvas: стекло MeshTransmissionMaterial, металл PBR по hex финиша (матовые — выше roughness), окружение через <Lightformer> (процедурная студия, БЕЗ внешнего .hdr — офлайн/CSP-safe), ContactShadows, OrbitControls. Partition3DView.tsx — dynamic import ssr:false (Canvas нельзя на сервере).
- Движок вынесен в components/configurator/ConfiguratorClient.tsx с variant 'internal'|'embed'. app/configurator/page.tsx — тонкая обёртка (internal, с ориентир. ценами материалов).
- Встраиваемый виджет: app/embed/shower/page.tsx + app/embed/layout.tsx (голый layout). variant='embed' — БЕЗ себестоимости (правило: финансы только CFO), спецификация без ₽, CTA «Оставить заявку» → window.parent.postMessage({type:'mglass-shower-config', config}). middleware.ts — добавлен isPublicEmbed (/embed/ публичный, по образцу isPublicDemo).
- Проверки: tsc чисто (вне сгенерённого .next), 5/5 тестов. Виджет отрисован в браузере (первый скрин: угловая распашная, дверь приоткрыта, петли+ручка, спека без цен, CTA) — 3D РАБОТАЕТ.

## Дорожная карта (согласована с владельцем)
Цель — качество каталожных эскизов public/configurator/renders/*.jpg (кабина в облицованной нише, реальная фурнитура, выноски). Фазы: 0 стабильный просмотр · **1 ниша+поддон (в работе)** · 2 реальная фурнитура (профили extrude + GLTF петли/ручки/ролики — владелец даст ЧЕРТЕЖИ с размерами) · 3 материалы под эскиз · 4 интерактив как privetmaket (клик по секциям, размеры в 3D, открытие двери) · 5 выноски-комплектация + экспорт эскиза · 6 прод/Tilda.

## Что сделано — РАСКЛАДКА → 3D (главное)
- Владелец подтвердил расположение 9 моделей: М1, М2, М4, М7, М8, М9, М10, М12, М11 (убраны М3, М5, М6, старый М12). Все распашные — НАРУЖУ. Трапеция = пятигранник (срез угла диагональю 135°, дверь на диагонали, петля стекло-стекло). М12 новая — центральная раздвижная (2 стационара + откатная створка).
- lib/configurator/arrangement.ts (новый) — ЕДИНЫЙ источник раскладки: 9 моделей, runs/segs (fixed/door/slide), правило петли (doorAttachment), constraints. Питает и схему, и 3D.
- Схема вид-сверху: docs/configurator/plan-schemes.html → артефакт https://claude.ai/code/artifact/fc644c25-da64-41ce-9e1a-15111dac024e (подтверждена владельцем).
- components/configurator/scene/assembly.ts: buildFromModel(model,dims,thickness) — строит 3D СТРОГО от раскладки (стены по shape, стекло вдоль ранов, дверь открывается наружу вокруг петлевой кромки, петля на стекле; пятигранник для trap). Старый buildAssembly оставлен, но НЕ используется.
- Partition3D/Partition3DView: сигнатура (model,dims,thickness,finishHex,finishId) вместо Configuration.
- ConfiguratorClient переписан на 9 M-моделей (список из M_MODELS, размеры из constraints, спецификация из assembly: стекло м² + петли/ручка/РД-001). Убраны catalog.ts computeConfiguration/PartitionIso из виджета.
- tsc чисто, тесты 19/19 (__tests__/configurator/buildFromModel.test.ts — все 9 строятся). УВИДЕЛИ М7 вживую: угловая, дверь наружу, петля на стекле, ниша, спека верна.

## ТЗ и документы
- ТЗ 3D-конфигуратора: docs/configurator/tz-3d-configurator.html → артефакт https://claude.ai/code/artifact/4e315424-499f-4abe-8f6b-c28ff1b8b71f
- hardwareSpecs.ts — реальные размеры фурнитуры (Balge-004, Dessau-103, SD-210, РД-001, штанга 30×10, Pr-002); фото/чертежи public/configurator/hardware/*.jpg.

## Сверка 9 моделей проведена (2026-08-18) + правки владельца
- WIP закоммичен (e18f28a чекпоинт), ветка запушена. Собран лист-сетка 3×3 всех 9 моделей вживую (через toDataURL канваса в браузер-панели; панель узкая → вёрстка в одну колонку, канвас ниже контролов, scrollIntoView + screenshot).
- Владелец сверил: раскладка в целом верна (6/9 читаются сразу). Замечания: (1) на раздвижных не видно фурнитуру; (2) стекло слишком прозрачное — сделать выразительнее.
- Исправлено (00ca5d2): каретки-ролики РД-001 на раздвижных (addCarriers в buildFromModel, model 'carrier', SlidingCarrier = обойма+2 ролика+планка); стекло заметнее (transmission 1→0.9, roughness→0.05, clearcoat→0.7, тон холоднее/менее зелёный #e6eef0). Проверено вживую: каретки видны на штанге, стекло голубовато-серое. tsc/eslint(тронутые файлы)/19-тестов зелёные.

## Следующий шаг
Ждём фидбэк владельца по размеру кареток/тону стекла. Незакрытые полировки из сверки (владелец пока не просил): М1 walk-in стоит без ниши/стены; М2 (распашная) и М10 (раздвижная) — створка визуально не отделена от стационара. Дальше по дорожной карте: Фаза 4 интерактив (клик по секциям, открытие двери, размеры в 3D), Фаза 5 выноски-комплектация + цена «от N ₽» (владелец выбрал формат «от N ₽»), Фаза 6 прод/Tilda. Просмотр: dev поднят на :3000, /embed/shower; при узкой панели — scrollIntoView канваса + screenshot, либо своё окно браузера.

## Что сделано — Фаза 3 (материалы, начато)
- Плитка ниши переделана: makeTileTexture — одна крупная плитка с тонким швом (низкий контраст) вместо резкой 4×4 шахматки; TILE_M 0.32; материал светлый #f2f0ea, roughness 0.85.
- Стекло чище: MeshTransmissionMaterial attenuationDistance 0.6→2.2, светлее (#f2f8f6), samples 6 / resolution 512.
- УВИДЕЛИ ЖИВЬЁМ (скриншот): угловая кабина в светлой плиточной нише, прозрачное стекло с плиткой за ним, петли+ручка, дверь приоткрыта — направление верное.
- Диагностирован и снят блокер: пустой экран был не багом кода, а СТУХШИМ бандлом dev-сервера (ошибка hingeCode из промежуточной правки; HMR-ws умер). Лечение: перезапуск dev-сервера начисто. Исходник корректен (tsc чисто).
- Просмотр: встроенная панель Browser схлопывается в innerWidth=0 → холст 0 → пусто; лечится resize_window 1280×820 ИЛИ смотреть в своём браузере (полное окно). Владелец дал Полный доступ к диску — EPERM ушёл.

## Что сделано — Фаза 2 (реальная фурнитура)
- lib/configurator/hardwareSpecs.ts (новый) — точные размеры с чертежей vetro-furniture: Balge-004 (скругл. 100×70, пятки 35, барабан, отв. Ø14/шаг45), Dessau-103 (прямоуг. 117×60, премиум), SD-210/L230 (П-скоба: хват 190, плечи 60, отв. Ø10/шаг210), РД-001 (Hip 30×10, ≤40кг/750/2100), штанга Ш-002 30×10, Pr-002 18×12,5. Фото/чертежи: public/configurator/hardware/*.jpg.
- components/configurator/scene/hardware.tsx (новый) — R3F-модели: Hinge (rounded/square по spec), HandleSD210 (плечи+хват), SlidingCarrier (ролик, упрощ. — точная по PDF позже). hingeModelFromCode: артикул→модель.
- assembly.ts: фурнитура вынесена в hardware[] (точки установки + модель balge/dessau/sd210); петли на петлевой кромке (пол-угла), ручка у внешней кромки двери, следует повороту. Крупные боксы петель/ручки убраны из metal.
- Partition3D: единый металл-материал финиша (hex) для профилей и фурнитуры; рендер assembly.hardware.
- tsc чисто (мои файлы), тесты 7/7 (добавлены на резолв модели петли и отсутствие фурнитуры у стационара).

## Что сделано — Фаза 1 (ниша)
- assembly.ts: тип Niche + расчёт (угловая = глухие стены сзади+справа; прямая = «стакан» задняя+2 боковые; глубина = width2 или 0.9 м дефолт; поддон 60 мм). bounds.d = глубина ниши.
- Partition3D.tsx: NicheMesh (пол/стены/поддон) с ПРОЦЕДУРНОЙ плиткой (CanvasTexture, офлайн), кабина поднята на поддон, камера/таргет на центр кабины, убран Center. Стекло теперь преломляет плитку → читается как настоящее. Хуки вынесены наверх (правило хуков).
- tsc чисто (мои файлы), тесты assembly 5/5.

## Следующий шаг
Визуально подтвердить Фазу 1 (нишу): открыть http://localhost:3000/embed/shower. Затем Фаза 2 — как только владелец пришлёт чертежи фурнитуры (петля Balge-004/Dessau, ручка SD-210, ролики раздвижной, сечения П-профиля и штанги 30×10). PDF каталога скопировать в проект (macOS блокирует ~/Downloads): `cp ~/Downloads/"Комплектации перегородок.pdf" ~/Desktop/КОД/КЛАУД/mglass-app/katalog.pdf`.

## Блокер просмотра (не код)
Встроенная панель Browser у владельца свёрнута → window.innerWidth=0 → canvas нулевой ширины (пустой серый прямоугольник), скриншот невозможен. Плюс dev-сервер нестабилен (RAM ~65 МБ свободно, Turbopack+Sentry, падает). Надёжный просмотр: открыть URL в СВОЁМ браузере (Safari/Chrome) ИЛИ прод-сборка (next build && next start — легче dev).

## Контекст
- Все 8 типов уже рендерятся в 3D (сцена читает config generically). Прототип-фокус: stationary, straight-swing, corner-swing.
- Гибрид по мировому опыту: стекло/профили — процедурно, петли/ручки — ПОКА простые боксы; следующий уровень — GLTF-модельки реальных петель (Balge-004/Dessau) + точные мм-сечения профилей (extrude).
- Встраивание в Tilda: iframe на /embed/shower + слушатель postMessage на стороне Tilda → в CRM. На проде задать NEXT_PUBLIC_EMBED_PARENT_ORIGIN (домен Tilda) вместо '*'.
- Dev-сервер на этой машине очень медленный (Turbopack+Sentry, компиляции 3+ мин) — не код.
- Ничего не закоммичено на этой ветке — по завершении визуальной проверки: commit + PR.

## Открытые вопросы
- Реалистичность петель/профилей: GLTF реальных артикулов или достаточно параметрических боксов? (влияет на «вау» для клиента).
- Нужна ли на виджете лид-форма (имя/телефон) ИЛИ postMessage в Tilda-форму достаточно?
- Цена клиенту: сейчас скрыта полностью — показывать «от N ₽» или только после заявки?
