## Текущая задача
Конфигуратор, вкладка «Сайт + 3D». Мержим ветку feat/shower-3d-configurator в main (A3 сцена + формы креплений M1 + B1 контракт).

## Что сделано (эта сессия)
- A2 (избранное «наши позиции» + сравнение поставщиков) — уехало в main через ветку прайса
- A3 сцена как рендер → components/configurator/Partition3D.tsx (отражающий пол, ACES, блики)
- A4 M1 конфигурируемость → assembly.ts (4 крепления + 2 обвязки, spec-коды) + ConfiguratorClient (UI) + формы креплений FDC-5D/FDK-5R (hardware.tsx)
- B1 клиент на новом контракте прайса → ConfiguratorClient: POST /options+/quote, KitPriceResult, choice(role→itemId)+qtyChoice
- fix(input): числовое поле Field — черновик-строка, зажим на blur/Enter (был clamp на каждую цифру)
- Координация 2 сессий → docs/CONFIGURATOR_COORDINATION.md, docs/configurator/M1_CONFIG_SPEC.md

## Следующий шаг
После мержа: пингнуть прайс, чтобы сняли шим GET /options. Дальше по маршруту — B2 (UX-шлифовка на новом контракте: подсказки к вариантам, порядок шагов, мобилка), затем B3 (куда доезжает заявка) и B4 (embed на Tilda).

## Контекст
Демо брать на M7/M2 (комплекты собраны полностью); ролики не заполнены → М8/М9/М10/М12 = «по запросу». Наполнение комплектов — админка KitPricingClient (прайс/Вера).
Себестоимость не в клиенте — цена с сервера. tsc 0, 385 тестов.

## Открытые вопросы
- B3: канал заявки (postMessage→Tilda→CRM или создание просчёта через /quote) — решить с владельцем.
- Визуал креплений M1 — плейсхолдеры-приближения, уточнить по фото при необходимости.
