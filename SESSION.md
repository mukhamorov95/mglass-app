## Текущая задача
Вкладка «Видео-обучение». Мерилка обучения в проде; заведён лог разбора чертежа —
до него на вопрос «запускали разбор или нет» ответить было нечем.

## Что сделано (эта сессия)
- `/admin/quote-quality` + `quote_quality_weekly(date)` — базовая линия обучения (#385)
- `scripts/narrate.mjs`: длительность считалась как для стерео, `say` пишет моно (#385)
- Очередь цеха: «размеры не указаны» больше не печатается над комментарием
  с готовыми диаметрами (#388)
- «Упаковано сегодня» / «Отгружено сегодня» + копирование в буфер (#388)
- Знак диаметра двух видов: Ø (U+00D8) 35 позиций и ∅ (U+2205) 30. Детектор
  ловил только Ø и терял 46%; расширен до `[ØøΦφ⌀∅]`, пересчёт 65 вместо 35 (#395)
- **Лог разбора чертежа** → `drawing_parse_log` (миграция ПРИМЕНЕНА, RLS вкл.,
  единственная политика — чтение admin/ceo; пишет только сервис-клиент).
  Обвязаны ОБА маршрута: `/api/ai/parse-drawing` и `/api/b2b/parse-pdf`,
  на успех и на каждый отказ. `lib/ai/parseSignals.ts` — чистые счётчики,
  `lib/ai/parseLog.ts` — запись (`server-only`), 5 тестов

## Следующий шаг
Ждать ответа оркестратора. Ролик учит «прогоните чертёж через разбор» — кнопкой
не пользуются Вера, Дмитрий и Яна; через неделю лога это будет видно точно.

## Контекст
Маршрутов разбора ДВА, и данные об отверстиях даёт второй:
`/api/b2b/parse-pdf` — только размеры (модель `claude-sonnet-5`);
`/api/ai/parse-drawing` — многодетальный, отдаёт `holes`/`cutouts`/`notes`,
из него и берутся диаметры в комментарии (модель `claude-opus-4-8`).
`notes.drawing_url` (`order-drawings/<id>.pdf`) — рабочий чертёж ДЛЯ цеха, не вход
разбора. Связи между ним и диаметрами нет и не было.
Лог пишем в свою таблицу, а не в `agent_logs`: там autonomous-агенты, и их читают
лента `/admin/agents` (select *) и `cron/agent-ceo` (последние 50 за день) —
десяток разборов в день вытеснил бы оттуда то, ради чего эти читатели написаны.

## Открытые вопросы
- Ссылка на `/admin/quote-quality` в Sidebar — за оркестратором
- `claude-opus-4-8` в 13 местах кода при 24 файлах уже на 5-й линейке;
  корневой `CLAUDE.md:84` называет 4-8 последней моделью. Решение по смене — владельца
- Экрана для `drawing_parse_log` нет: данные копятся, смотреть пока только SQL
