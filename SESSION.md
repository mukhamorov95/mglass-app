## Текущая задача
Всё смержено и в проде. Следующее — контур «Проблемы» (#152, #153) или оплата в розничных заказах (#149).

## Что сделано (эта сессия)
- Монтаж из заказа/КП → форма `/installations` предзаполненной → `components/AssignInstallationButton.tsx` (PR #67)
- Монтажи видны во внутреннем `/calendar` (читает `installations` рядом с `appointments`) → `app/calendar/page.tsx` (PR #67)
- Мост «счёт → деньги → Отдел продаж»: миграция `crm_sales.contract_id`, `upsertSaleFromContract`, `contractPaymentKey`, `POST/GET/PATCH /api/contracts/[id]/payment` (PR #68)
- Карточка поступлений в истории договоров → `components/ContractPaymentsPanel.tsx` (PR #68)
- Отдел продаж только M-Glass (`voided=false AND department≠'b2b'`) + разделены «продано» и «поступило» → `app/api/sales/route.ts`, `app/sales/page.tsx`
- Цех: деталь ≠ задача (счётчик карточки, табло мастера, подписи кнопок) → `app/production-app/my-queue/page.tsx`
- Изделия производства теряли габариты (нули жёстко в коде) → `lib/b2bFactoryProducts.ts`; бэкфилл 15 позиций в 8 заказах из размеров в комментарии (сверено по площади — совпало во всех)
- Единый ярлык материала → `lib/materialLabel.ts` (PR #70): цех видел «Осветлённое 4 мм» и не мог отличить зеркало (id 49) от стекла «Осветлённое CrystalVision». Подключено в производственное сообщение, карточку заказа, очередь мастера, «Заказы», проверку/потребность материала, наклейки, раскрой, КП. 9 тестов. Проверено в проде на заказе 005094 → «Зеркало Осветлённое 4мм»
- Фикс индекса: `ON CONFLICT (contract_id)` не подхватывал частичный индекс → обычный уникальный (`20260728_crm_sales_contract_link.sql`, PR #69). Поймано владельцем на живом клике

## Следующий шаг
Владелец мёржит PR #67 и #68 → деплой Vercel → сквозная проверка в Chrome.
Затем контур «Проблемы» (задачи #152, #153) — см. Контекст.

## Контекст
**Проблемы в цехе (не начато, разобрано).** Кнопок «Проблема» ДВЕ, пишут в разные места:
- `my-queue:742` и `production-app/orders:248` → спрашивают причину (11 кодов `ANDON_REASONS` в `lib/productionRouting.ts:107`), пишут в `production_tasks` через `/api/production-tasks/[id]`
- `app/p/o/[orderId]:697` (по QR) → **не спрашивает ничего**, пишет в `b2b_orders.notes.detail_stages`, и этот блоб **намеренно исключён** из синка (`sync-stages/route.ts:47`)

Проблема Никиты: заказ 4992, поз. 3, 27.07 10:19, nikita@mglass.ru, без причины, стоимость 9 388 ₽ — единственная открытая в базе, увидеть её было негде (`/today` не выведен в меню, `/supervisor` только для владельца, уведомлений нет).

Нужные владельцу причины УЖЕ есть в CHECK-констрейнте (`tempering_defect`, `scratch`, `cut_defect`, `drilling_defect`, `other`) — миграция для них не нужна. Нужно: `problem_by`/`problem_resolved_by`, экран `/production-app/problems` (канбан + сумма), действие «решено», вкладка в `ProductionTabs.tsx:12` и `Sidebar.tsx:313`, запрет удаления (сейчас крестик на плитке `production-app/orders/[id]:433` доступен всем, и RLS позволяет рабочему затереть историю).

Сумма проблем: `manualTotal ?? round(saleIncVat × (1 − discount/100))`, **дедуп по (order_id, item_index)** — иначе завышение в ~8 раз.

## Открытые вопросы
- PR #67, #68 — мёрж только вручную владельцем
- Розница `/orders/[id]` блок «Оплата» всё ещё не пишет в денежное ядро (задача #149, документированный шаг Д2)
- Три надиктованных сообщения (6000 К; зеркало Crystal Vision с аурой; ответ Татьяне) — похоже, ушли не в то окно, действий не предпринято
