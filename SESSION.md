## Текущая задача
Голосовая диктовка не работала → диагностирован исчерпанный OpenAI-квота; добавлен фолбэк Groq + понятные ошибки. В проде (PR #65, bc6c28c).

## Что сделано (эта сессия)
- КП: цена одной суммой (PR #63)
- Доступ Веры: пространство «Менеджер» + вкладки (PR #64)
- Голос (PR #65): lib/transcribe.ts (OpenAI→Groq фолбэк, коды ошибок), подключён в
  installations/parse-voice, accounting/notes, vlad/notes, ai/kp-transcribe;
  6 тестов. Диагноз: OpenAI Whisper HTTP 429 insufficient_quota (проверено локально
  ключами из .env.local: Whisper 429, Claude 200).

## Следующий шаг
Ждать решения владельца по STT: пополнить OpenAI ИЛИ добавить GROQ_API_KEY в env
(Vercel). После любого — диктовка заработает везде.

## Контекст
- Исчерпанная квота OpenAI ломает ВСЮ Whisper-диктовку (монтаж/КП/бухгалтерия/Влад)
  и разбор звонков (amo/calls/*). Разбор Claude не затронут.
- GROQ_API_KEY бесплатный (console.groq.com), Whisper large-v3-turbo. Модуль
  transcribeRu подхватит его автоматически как запасной (или основной, если нет OpenAI).
- Остались на прямом OpenAI (не через модуль, фолбэка нет): amo/calls/transcribe и
  amo/calls/analyze — фоновые, при желании тоже перевести на transcribeRu.

## Открытые вопросы
Диктовку вживую (микрофон→запись) под ролью сотрудника не проверить из сессии;
пайплайн подтверждён поэлементно + юнит-тестами.
