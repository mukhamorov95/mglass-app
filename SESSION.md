## Текущая задача
Вкладка «Видео-обучение». Мерилка полноты просчёта сделана; сценарий первого ролика
разобран, в инструменте озвучки найдена и починена ошибка длительности.

## Что сделано (эта сессия)
- Разведка перед кодом → аудитория оказалась не цех, а менеджеры (поправка оркестратора);
  инфраструктуры видео в проекте нет вообще (ни `<video>`, ни embed, ни видеобакета)
- `supabase/migrations/20260902_quote_quality.sql` (ПРИМЕНЕНА, проверено по `pg_proc`:
  `prosecdef=false`, acl `{postgres,service_role}`) — функция `quote_quality_weekly(date)`
- `lib/b2b/quoteQuality.ts` — чистые свёртки: доли, сводка по неделям и менеджерам,
  `groupsFieldExisted()` (поле групп ⌀ выкачено 28.08, до этого ноль ничего не значит)
- `app/api/admin/quote-quality` (requireOwner + сервис-клиент) и `app/admin/quote-quality`
- 9 тестов → `__tests__/b2b/quoteQuality.test.ts`; всего 816/816 зелёные, билд зелёный
- `scripts/narrate.mjs` — длительность бралась из размера файла по формуле для СТЕРЕО,
  а `say` пишет МОНО: каждый слайд держался ровно половину нужного времени. Теперь
  спрашиваем `afinfo`. Реальная озвучка первого ролика — 109 с, а не 55 с
- `public/narration/calc-whats-new/manifest.json` пересобран (все значения удвоились)

## Следующий шаг
Ждать решения оркестратора по двум находкам: (1) в 37 позициях диаметры уже лежат в
`comment` от разбора чертежа, а поле групп пусто — ролик просит перепечатать то, что
система знает; дешевле парсер `comment → holes[]`, чем обучение. (2) Сценарий
`content/video/calc-whats-new.json` фактически верен, но s01 обещает «три минуты» при
109 с, и слайды s04/s05/s08 учат ручному вводу тех самых диаметров.

## Контекст
Метрика намеренно из ДВУХ разных счётчиков: признак `hasHoles` (живёт с 30.06, ноль в нём —
это ноль) и группы `holes` (с 28.08). Третья колонка — диаметры в комментарии — меняет
диагноз с «менеджер забыл» на «его просят перепечатать».
Экран НЕ ловит случай 01.09 (4 заказа, 7 позиций, ни признака, ни комментария): в данных
он неотличим от изделий, которым сверловка не нужна. Это написано на самом экране.

## Открытые вопросы
- Ссылку на `/admin/quote-quality` в `components/Sidebar.tsx` ставит оркестратор (его файл)
- Второй ролик («полный алгоритм просчёта») не снимать, пока не измерим первый
