## Текущая задача
Кабинет менеджера: маршрут «путь сделки», пять шагов от заявки до зарплаты.
Владелец уехал, работаем автономно — вкладка идёт шаги 1–4 подряд без остановок,
оркестратор проверяет в проде и делает шаг 5 (меню) последним.

## Что сделано (эта сессия)
### Калькуляторы — закрыто
- Пять экранов с нечестными ценами убраны в архив (#425): зеркало, душевая,
  лофт, ограждение, скан. Причина: у душевой фурнитура бралась из таблицы
  в одну строку, остальное — фолбэком `hardware_base × 0.6`. Занижение до +99%
  (М11), +87% (М1). Код на месте, вернуть = вернуть строки в ROLE_ALLOWED и меню.
- Три калькулятора: «Быстрый расчёт», «Расчёт» (#432, модель → проём →
  реальный BOM), «Расчёт B2B» (#428, без клиента и срока, панель цены справа).
- Стекло пер-панельно через раскрой (#445). Сверено двумя путями: 2686 ₽/м²
  и в «Расчёте B2B» вручную, и в композиции движков. Флэт по стеклу завышал
  (6600 → 5910), по фурнитуре занижал.
- Higgsfield в `paid_services` + раздел «Продвижение» (#436): пульт с балансом
  и регламент на 11 шагов. Баланс обновляется вручную — ключа в приложении нет.

### Прогон в проде — 5 находок, все закрыты
«Открыть» роняло страницу расчёта; на «Расчёте» не было сохранения; в быстром
не было полей клиента; сохранение молчало; доставка входила в сумму без строки.

## Следующий шаг
Шаг 1 маршрута: `deal_id` у КП/договоров/счетов, вкладки «Документы» и «Деньги»
в `/deal/[id]`, кнопки создания из карточки. Ведёт вкладка кабинета менеджера.
Договорено: КП и договор — переход с префиллом (механизм `mglass_kp_prefill`
уже есть), счёт — прямо из карточки (образец в `app/b2b-deal/[id]`).

## Контекст
- На `deals` сегодня ссылается ТОЛЬКО `calculations`. КП 72, договоров 26,
  счетов 21 — со сделкой не связаны. Заявок на замер 2, замеров 0.
  Вывод: сшивать построенное, а не строить.
- Наценка ложится дважды НАМЕРЕННО: производство продаёт M-Glass по B2B-цене
  с НДС (скидка M GLASS 20%), розница накручивает 40% и 12% сверху. Это две
  сделки между двумя юрлицами, не двойной счёт. Маржа цеха на заказах
  M GLASS — 49,3% на 275 позициях, скидка её не съедает.
- Мина в `lib/quickCalc.ts` (ветка shower, budget) — та же занижённая
  фурнитура, живой путь: CRM, телеграм-бот, Авито, генератор КП. Потока сейчас
  нет (0 сообщений AI за 30 дней), чиним вместе с переключением на BOM.

## Открытые вопросы
- Новый вид экрана «Расчёт» (два экрана: выбор модели → 3D слева + параметры
  справа + цена) — ТЗ записано в базу знаний владельца, делаем после маршрута.
- Higgsfield 0,44 кредита: студия, публикация и бюджет раздела «Продвижение»
  не строятся до пополнения.
- `HIGGSFIELD_API_KEY` в env — тогда баланс будет обновляться сам.
