## Текущая задача
Складской контур /inventory ПОСТРОЕН ЦЕЛИКОМ и в проде. Активной кода-работы нет —
контур упирается в наполнение (inventory_items=0), это действие владельца, держит backbone.

## Что в main (складской контур)
- #237 контур: inventory_items + inventory_moves, остаток = сумма движений (триггер),
  журнал неизменяем, единицы тара↔база (стекло листы→м², профиль хлысты→м.п.)
- #287 С4 резерв: inventory_reservations, qty_reserved (триггер), reserveForOrder(...),
  best-effort + идемпотентно; резерв ≠ расход (qty не трогается)
- #305 списание по факту из цеха: OrderConsumePanel (встраиваемый), applyConsume
  порядок «движение→закрытие резерва», частичный расход возвращает остаток брони
- #307 origin: движение несёт 'fact'|'plan' (ортогонально reason); автосписание пишет plan,
  инвентаризация видит расхождение; consume 409→200 alreadyConsumed
- #312 consumeForOrder(docType,docId,by,origin='plan') в lib/inventory/consumeHook.ts:
  единая server-only точка автосписания (import 'server-only'), best-effort, идемпотентно,
  by→created_by_name; addMoves/applyConsume ослаблены до MoveActor {userId?,name?}

## Стыковки завершения заказа (обе зовут consumeForOrder, origin='plan')
- b2b-orders (ручной packaged): PR #313 — стыковку одобрил, у backbone на мерже.
  Вызов на постановке packaged в дату, best-effort, атрибуция менеджер→email→'b2b-orders (авто)'
- производство (packaged в mirrorOrderStages): их волна, П19 — сначала чинят гонку notes
  (атомарный mark_order_stages), потом вешают вызов. Точка согласована.

## Следующий шаг
Ждём владельца (держит backbone), обе задачи НЕ на мне:
1. Наполнение: /inventory → «Из справочников» → всё → «Инвентаризация» (реальные остатки знает цех)
2. Разово или партиями берут материал под заказ — определит, включать ли автосписание дальше
   пометки origin (инкрементальный режим = другой контракт, ключ идемпотентности на подачу)

## Контекст
- Доступ: смотрят admin/ceo/buyer/production/manager/commercial/cfo; двигают admin/ceo/buyer/production;
  себестоимость видят admin/ceo/buyer/cfo (API обнуляет остальным). Автосписание — системный
  side-effect, идёт мимо requireInventoryWrite через server-only consumeForOrder (backbone одобрил
  как НЕ эскалацию: прецедент reserveForOrder в launch-production, прямая правка гейтится как была)
- «На руках» будет завышен на цикл производства (резка↔упаковка ~10 сут) — доступный остаток
  (qty−qty_reserved) верен всегда, завышение помечено origin=plan. Размен принят всеми.
- Открыто у backbone: (а) ~270 старых заказов без задач — историю не догоняем; (б) точность «на руках»
  = настоящий триггер закрытие резки (отдельный разговор)
- Совместный живой прогон origin='plan' с b2b-orders — за мной, когда засею тестовую позицию
  с остатком + bom_alias под реальный заказ

## Открытые вопросы
- Реальные минимумы/нормы по позициям — знает цех
- Длины хлыстов профиля (2000/2200/3000) — влияют на pack_size погонных
