## Текущая задача
Производственный маршрут: признаки изделия из просчёта → этапы цеха. Сделано.

## Что сделано (эта сессия)
- Фацет и пескоструй у зеркала с подсветкой — разные обработки, с предупреждением
  «себестоимость не заведена» → lib/b2bFactoryProducts.ts, app/calculator/b2b/page.tsx (#349)
- Сумма заказа поднимается, а не только опускается — форма отвергала повышение,
  хотя роут его принимал → app/b2b-orders/page.tsx (#350)
- Рабочий видит весь маршрут детали и не заперт чужой неотметкой
  → app/production-app/my-queue/page.tsx (#351)
- Песочка — отдельный этап маршрута, включается галкой в просчёте
  → lib/productionStages.ts, lib/productionRouting.ts, app/calculator/b2b/page.tsx (#352)
- Криволинейное зеркало больше не уходит в цех как прямое (было зашито shape:'rect')
- Станции в /admin/users выводятся из PRODUCTION_STAGES, а не из своей копии

## Следующий шаг
Зеркало с подсветкой, оставшееся из спецификации владельца:
- аура / фронтальная (песочка) как назначение подсветки + 4 работы песочки
- подогрев за штуку, с предупреждением о незаведённой себестоимости
- кнопка = тип (взмах/нажатие) × диммер (два измерения вместо одного списка)
- отверстия: пары «диаметр × количество»; вырезы внутренние/наружные
Требует новых строк ставок в БД — Supabase MCP отключён, миграцию не применить.

## Контекст
Цепочка цеха: Бека (резка) → Ильзат (полировка) → Адилет (сверловка) → Никита.
Принцип владельца: неотмеченный этап не значит несделанную работу.
Каскад auto_closed уже был в lib/productionCascade.ts — заперт был только экран.

## Открытые вопросы
- Supabase MCP отключён: ставки песочки/подогрева/диммера завести нельзя
- Эльзат и Адилет не подключены (ссылки готовы, 7 дней) — блокирует П7/П8/П11/П12/П15–П17
- Судьба Сергея и Валерии не решена
- Габариты листов в справочнике (Шиншилла: в имени 2100×1600, режется 3210×2250)
