## Текущая задача
Складской контур `/inventory`: единый реестр остатков B2B (стекло/зеркало, м²/листы) и B2C
(фурнитура, шт/м.п./хлысты) с журналом движений и интеграциями. Готов, ждёт заполнения данными.

## Что сделано (эта сессия)
- Миграция ядра склада → supabase/migrations/20260827_inventory_core.sql (ПРИМЕНЕНА):
  `inventory_items` + `inventory_moves`, остаток и средняя себестоимость ведёт триггер,
  журнал неизменяем, повтор списания по документу блокирует уникальный индекс
- Единицы и статусы → lib/inventory/units.ts (тара ↔ база: лист 3210×2250 = 7.2225 м², хлыст 2.2 м)
- Сопоставление BOM ↔ склад → lib/inventory/match.ts + plan.ts (ref → alias → имя; услуги отсеиваются)
- Слой данных → lib/inventory/db.ts, гейт ролей → lib/inventory/auth.ts
- API → app/api/inventory/{items,moves,count,summary,catalog,consume,documents,purchases}
- Раздел → app/inventory: Остатки / Приход / Инвентаризация / Списание по заказам / Движения
- Крон дефицита переведён на новый реестр → app/api/cron/stock/route.ts
- `/admin/warehouse` и `/admin/stock-control` → редирект на `/inventory` (один источник правды)
- Сайдбар + ROLE_ALLOWED: склад виден снабжению, производству, менеджерам, коммерческому, CFO
- 20 тестов на единицы и планы списания → __tests__/inventory (всего 405/405 зелёные), билд зелёный
- Описание контура → docs/INVENTORY.md

## Следующий шаг
Завести позиции: `/inventory` → «Из справочников» → «Выбрать всё» (75 B2B-материалов,
96 позиций фурнитуры, 11 материалов B2C) → проставить минимумы и провести первую инвентаризацию.

## Контекст
Остаток НЕ редактируется руками — только движением. Списание по заказу всегда с предпросмотром
и по кнопке, автосписания нет сознательно. Непонятые строки BOM привязываются к позиции прямо
в предпросмотре (уходит в `bom_aliases`) — дальше заказ узнаётся сам.
Себестоимость (`avg_cost`, стоимость запаса) видят только admin/ceo/buyer/cfo, остальным API
обнуляет поля.

## Открытые вопросы
- Реальные минимумы и нормы по позициям — знает только цех, нужно проставить
- Длины хлыстов профиля (2000 / 2200 / 3000) — влияют на `pack_size` погонных позиций
- `qty_reserved` не заполняется: резерв под запущенные заказы не реализован
- Приход по закупке не двигает статус закупки в `/admin/procurement`

## Параллельно в main
Сессия «Менеджерский контур» доделала ручную корректировку ИТОГА просчёта в /b2b-quotes
(lib/b2b/priceOverride.ts, наложение manualTotal, прайсовая база не перезаписывается) и
обогащение справочника цен (lib/supplier/enrich.ts). Файлы не пересекаются со складом.

Другая сессия положила в main спеку склада docs/WAREHOUSE_ARCHITECTURE.md (кода в ней нет).
Реализация в этой ветке закрывает её шаги С1, С2, С3, С5 (приёмка) и С6 под именами
`inventory_items` / `inventory_moves` — сверка имён и остатка спеки в docs/INVENTORY.md.
Не сделаны: резервы под заказ (С4) и заявки на закупку из дефицита.

## С4 — резерв под заказ (ветка claude/inventory-reserve-c4, PR #287)
- Миграция inventory_reservations (ПРИМЕНЕНА) + триггер qty_reserved
- reserveForOrder(docType, docId, items) → { reserved, shortages, alreadyReserved };
  best-effort, идемпотентно; резерв ≠ расход (qty не трогается)
- releaseReservation / markReservationConsumed / listReservations; applyConsume закрывает резерв
- HTTP /api/inventory/reserve (POST/GET/DELETE); резерв виден в таблице остатков; 4 теста
- Вызов из launch-production вживляет backbone-сессия (её файл) — отдан контракт + сигнатура
- НЕ мержу: backbone (mglass-app-83) — гейт приёмки, ждёт вердикт по #287

## Списание по факту из цеха (ветка claude/inventory-shopfloor-consume)
- reserved как дефолт количества: PlanRow.reserved + attachReserved в buildConsumePlan
- applyConsume: сначала движение расхода, потом закрытие резерва (active→consumed весь заказ);
  частичный расход возвращает остаток резерва в доступное; вернул released
- OrderConsumePanel.tsx — встраиваемый компонент для производственного контура (только количества,
  себестоимость скрыта, тихо на пустом складе, идемпотентно через unique index)
- 3 теста; проверен полный цикл на боевой базе (резерв→частичное списание 3→резерв consumed→повтор блок)
- Вживление кнопки в экран рабочего — за производственным контуром (его файлы), отдам контракт
- ОГРАНИЧЕНИЕ вынес в вопрос: списание разовое (закрытие заказа), не инкрементальное партиями

## Признак происхождения движения (ветка claude/inventory-move-origin)
- Миграция inventory_moves.origin 'fact'|'plan' (ПРИМЕНЕНА), дефолт fact, индекс на plan
- origin протянут через NewMove/addMoves/applyConsume/listMoves; applyConsume(...,'plan')
- POST /api/inventory/consume: origin в теле; «уже списано»/«нечего списывать» → 200 (не 409),
  флаги alreadyConsumed — под автосписание из цеха (не мигать сбоем в логах)
- Журнал помечает plan-движения бейджем «по плану»; панель супервайзера = fact (исправление)
- Требование backbone: автосписание пишет ПЛАН, а не факт → помечено, инвентаризация видит расхождение
- Автосписание дальше пометки НЕ реализую до ответа владельца (разово/партиями)

## Стыковка с производством (production-tab-analysis-739b51-0a)
- Точка вызова: переход флага packaged в mirrorOrderStages (не каждый вызов), packaging всегда последний
- Контракт финал: POST /api/inventory/consume { type:'b2b_order', id, origin:'plan' }, best-effort, 200 на пустом/повторе
- Открыто (к backbone): (a) ~270 старых заказов без задач — историю не догоняем; (б) packaged руками из /b2b-orders мимо цеха — точка вкладки b2b-orders; (в) «на руках» завышен на цикл (резка↔упаковка ~10 сут) — доступный остаток верен, вопрос точности к владельцу
- Производство сначала чинит гонку notes (атомарный mark_order_stages), потом вешает вызов; П19 последняя волна

## Ожидание (после #307)
- #307 (origin) CI зелёный, конфликтов нет — ждёт мержа backbone
- Два хука списания зовут POST /api/inventory/consume {type,id,origin:'plan'}, best-effort:
  производство (packaged в mirrorOrderStages, роль production — HTTP-гейт проходит) +
  b2b-orders (ручной packaged, роль manager — HTTP-гейт 403!)
- ACCESS-MODEL вопрос вынесен backbone: consume гейтится requireInventoryWrite (нет manager).
  consume-на-packaged = системный side-effect (прецедент: reserveForOrder в launch-production,
  где manager разрешён). Решение НЕ открываю сам — жду backbone.
- Как backbone подтвердит: добавлю server-only обёртку consumeForOrder(docType,docId,by,origin='plan'),
  импортируют ОБА хука; единая семантика, идемпотентность по (заказ,позиция), best-effort.
- b2b-orders (сессия ...9dd210-94) предупреждён: не вешать до (1) #307 в main и (2) access-model от backbone.
