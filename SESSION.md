## АУДИТ ВСЕЙ СИСТЕМЫ — 11 РОЛЕЙ (2026-07-06, последнее)
`5d8f3c9`. Полный отчёт: **docs/SYSTEM_AUDIT_2026-07.md** (11 ролевых аналитиков по коду: CEO/CFO/РОП B2C/B2B/производство/монтаж/закупки/логистика/маркетинг/архитектор/ИБ; в шапке файла — приоритетный план этапов 0-6). Ключевое: Этап 0 чистка дублей (3 менеджер-дашборда→1, production×4→1, b2b-crm мёртв, kp×2, warehouse→stock-control, allowlist≠меню, заглушки procurement-routes/competitors/product-finder/templates/academy); Этап 1 деньги (дебиторка B2B+B2C, ДДС/платёжный календарь, один источник выручки orders vs calculations); Этап 2 монтаж (контур отсутствует целиком); Этап 3 производство-2 (план недели, нормы, брак); Этап 4 закупки/логистика (замкнуть заявка→заказ→остатки; таблиц procurement_routes НЕТ в БД); Этап 5 ИБ (подписи вебхуков, RLS calculations/orders, cron); Этап 6 маркетинг (сквозная аналитика, заглушки убрать). Владелец решает, с какого этапа начинать.

## CFO: ФИНМОДЕЛЬ ТБ (ХАББАРД) — /cfo/breakeven (2026-07-06)
`decc08f`, прод, ПРОВЕРЕНО Chrome end-to-end. Миграция 20260706_finplan_models.sql применена мной через Chrome SQL Editor (Success). Проверено живьём: цифры совпали с xlsx (маржа 37.2%, ТБ-0 9 718 040, остаток −378 690); ввод фонда 5% → ТБ-1 10 229 516 ✓; доход собственника 300 000 ₽ → ТБ-цель 11 078 460 ✓; Сохранить → reload → данные из БД ✓; тестовые значения возвращены к нулям и сохранены; юнит «Производство» ✓ (ТБ-0 984 446, остаток +718 960, план 244% от ТБ).
- Источник: xlsx владельца «Мгласс_производство_переменные» (5 вкладок, все цифры сняты и посеяны в DEFAULTS).
- Модель (структура таблицы сохранена): Доходы по видам (стекло 2.4М / изделия 6.3М) → Переменные % от СВОЕГО дохода (построчно, стекло Σ49.21% / изделия Σ67.98%) → Маржа per вид и общая → Фонды от маржи % (возврат инвестиций / обучение / резерв / НОВЫЙ фонд бонусов производства) → Сумма на распределение → Постоянные ₽ (21 строка, Σ3 614 910) → Остаток.
- Выходы: ТБ-0 = Постоянные/маржа% (без фондов); ТБ-1 = Постоянные/(маржа%×(1−фонды%)); ТБ-цель = (Постоянные+доход собственника ₽)/(маржа%×(1−фонды%−собственник%)); прогресс-бары план vs ТБ; Фонд перелива = остаток после всего, из него % → бонусы производства.
- **Разбивка перелива на юнитах (`897582a`)**: блок «бонусы % / погашение» показывается и на вкладках M-Glass/Производство — единый % компании (ovCfg) применяется к переливу каждого юнита; поле «Тело долга» только на Компании 0. Build ✓, запушено; Chrome отвалился до визуальной проверки — при случае глянуть вкладку юнита (перелив производства/M-Glass × 20%).
- **Фонд перелива с распределением (`13345a9`)**: на сводках блок перелива интерактивный — регулируемый «% → бонусы производства» (владелец сохранил 20%), остальное «→ досрочное погашение обязательств»; опц. поле «Тело долга» → срок закрытия переливом сверх плановых платежей (debtMonthly = Σ строк кредит/лизинг юнитов). Настройки хранятся в строке unit='total' ({overflowBonusPct, debtBalance}) — строка реюзнута, кнопка «Сохранить настройки перелива» в самом блоке. Юнитовое поле overflowBonusPct убрано из формы (в данных осталось, не используется). Проверено на проде: перелив 2 418 883 → бонусы 483 777 (20%) + погашение 1 935 106 (80%), persist после reload ✓. Двухуровневая премия цеха: фонд бонусов производства (% от маржи, стабильная часть) + доля перелива (ускоритель в хорошие месяцы; ниже ТБ перелива нет — бонусов из него нет).
- **«Компания 1 · без кредитов и лизинга» (`c711109`)**: четвёртая вкладка — та же автосумма юнитов, но из постоянных исключены строки с «кредит»/«лизинг» в названии (regex по имени строки — при переименовании статей учитывать). Проверено на проде: постоянные −690 000 к «Компании 0» (кредит 290к + лизинг 400к), при данных владельца остаток +698 983, ТБ-0 6 456 322 (план 158% от точки).
- **Архитектура вкладок (`89d3dda`)**: редактируются ДВА юнита — «Производство» и «M-Glass» (каждый — своя строка finplan_models). Вкладка «Компания (всё)» = АВТОСУММА юнитов, read-only: доходы/переменные зеркалятся из юнитов, маржа складывается, одноимённые фонды суммируются (в UI показан эффективный % от общей маржи, ₽ = Σ юнитов), постоянные — двумя группами «Производство»/«M-Glass», доход собственника = Σ (ownerRub + ownerPct×маржа юнита). Строка unit='total' в finplan_models БОЛЬШЕ НЕ ЧИТАЕТСЯ (select .in mglass,production) — лежит мёртвой, можно удалить.
- Сиды юнитов из файла «оклады_лизинг_кредит_финал.xlsx» (`1053291`): лизинг 400 000 → производству, кредит 290 000 → MGlass, оклады 1 420к = 390к + 1 030к, аренда 750к = 250к + 500к. Владелец УЖЕ внёс свои живые цифры в юниты и сохранил (доходы 4М/8М, свои %, фонды ~23.8%, постоянные Σ3 571 710) — сводка агрегирует их корректно (проверено на проде: маржа 5 200 000 · 43.3%, фонды −1 240 000, остаток +388 290, ТБ-1 10 823 364). Фикс `edc6cfa`: поле названия дохода сжималось в ноль (w-full у числового поля), голосовой ввод владельца попал в название → вёрстка исправлена, названия доходов «Производство (B2B) — продажа стекла» / «M-Glass (B2C) — изделия с монтажом», мусор вычищен из БД jsonb_set'ом.
- Всё редактируемое (синие поля), строки добавляются/удаляются, upsert в finplan_models (jsonb per unit, RLS admin/ceo/cfo). Меню: CFO → «🎯 Точка безубыточности» (оба места Sidebar).
- Файлы: app/cfo/breakeven/page.tsx (новая), components/Sidebar.tsx (CFO_ITEMS), supabase/migrations/20260706_finplan_models.sql.
- Следующий шаг: владелец применяет SQL → проверка Chrome (загрузка/правка/сохранение) → позже вынести часть (без денег собственника) в производство для рабочих.

## B2B: ИЗДЕЛИЯ ПРОИЗВОДСТВА В КАЛЬКУЛЯТОРЕ (2026-07-06)
`3a34eee`+`9de348c`, прод, ПРОВЕРЕНО Chrome end-to-end (свежая вкладка — старая деградировала, CDP-фризы). Зеркало 1100×1960: себест 7 910 → цена производства 13 638 = 7910/(1−0.30−0.12) — конфиг mirror в БД (маржа 30/налог 12); комплектация авто = как в рознице (LUX 120 led 9.6W, БП КАРАНДАШ 100W); позиция «изделие» добавлена (2,156 м², 21,6 кг, маржа 42%). Лофт 2000×2400 3×2 глухая: селект стекла из синтеза матрицы (fix 9de348c: retail materials без «стекло» → синтез из glass_price_matrix cost, как /calculator/loft); себест 13 135 → цена 27 365 = 13135/0.48 (fallback 40/12). Черновик очищен.
- Стратегия владельца: производство — внутренний поставщик; M-Glass покупает готовые изделия и монтирует. B2B-калькулятор считает изделия по ЦЕНЕ ПРОИЗВОДСТВА: production stage финмодели V2 = factoryCost / (1 − production_margin − production_tax), конфиг pricing_model_config_v2 (mirror/loft; fallback 40/12).
- lib/b2bFactoryProducts.ts: loadFactoryData (лениво: retail materials/services, glass_price_matrix COST-строки, mirror_lighting_components, financial_settings, pricing-конфиг), calcFactoryMirror (calculateMirrorUnified + factory overhead/scrap/packaging; стандартная комплектация: первый профиль, лента 12V, БП по мощности, рассеиватель), calcFactoryLoft (calculateLoft: металл+стекло+закалка+покраска), factoryQuoteToItem → B2BOrderItem (category «изделие», width/height=0 → не попадает в раскрой партий, НДС 22).
- UI: сегмент «Тип позиции»: Стекло/зеркало | 💡 Зеркало+свет | Лофт; формы изделий; карточка «Себестоимость производства → Цена производства (маржа N%)» + spec; «+ Добавить изделие» → обычный поток (просчёт→КП/Счёт→производство).

## MGLASS КАЛЬКУЛЯТОР ЗЕРКАЛ: ЕДИНАЯ ЦЕНА (ФИКС ОТ МЕНЕДЖЕРА) (2026-07-06)
`c6d486b`, прод, проверено Chrome на параметрах менеджера (1100×1960, Серебро 4мм).
- Баг: «Цена клиенту» = финмодель V2 (36 248 с услугами / изделие 29 748), а «Текст КП» и заголовок «Расчёт цены» = legacy live из mirrorCalculator.clientText (25 958 / 19 458). Менеджер копировал клиенту старую цену.
- Фикс (только page.tsx, mirrorCalculator не тронут): displayClientText — при доступной V2 в тексте КП строки «Стоимость изделия/Итого с услугами/Стоимость» подменяются на productQuotePrice/quotePrice. Применён в рендере, копировании и сохранении client_text (handleSave + addToCart). Заголовок «Расчёт цены» = quotePrice.
- Проверено: все три места показывают 29 748 ₽ одинаково. При недоступной V2 поведение прежнее (live).

## ЦЕХ: ИДЕИ + КУПИТЬ + ДЕМО-ЗАКАЗ + ШПАРГАЛКА (2026-07-06)
`cbde026`, прод. Для планёрки цеха.
- **💡 Идеи** (/production-app/ideas): голосовой ввод (Web Speech ru-RU) → /api/ai/parse-idea (Claude: problem/solution/hint) → отправка → история «Обращение №N» (RLS: рабочий видит только свои). Статусы new/review/accepted/implemented/rejected.
- **Оценка руководства** (/admin/ideas, Админ→Операции→Идеи цеха): все обращения + скрытый блок (заметки/эффект ₽мес/доказательное обоснование/премия) в production_idea_reviews — RLS только admin/ceo.
- **🛒 Купить** (/production-app/buy): заявки на расходники; вкладки Новая/В работе/Выполненные; канбан Необходимо купить→Заказано→Приехал на склад; кто создал/заказал/принял (имя+время); ссылка на товар; комментарии-обсуждение.
- Меню: Цех+«Команда» (Идеи), Материал+Купить, верхние вкладки цеха += 💡/🛒. Миграция 20260706_ideas_and_purchases.sql ПРИМЕНЕНА (2026-07-06, Chrome SQL Editor, Success — до этого таблиц не было, экраны не работали); /production-app/ideas грузится без ошибок («Мои обращения · 0»).
- **ДЕМО-заказ #4897 «ДЕМО-1» (УЧЕБНЫЙ)** создан и прогнан для планёрки: 2 поз. (стекло 8мм закалка+отверстия; зеркало 4мм фацет), 9 задач, резка обеих ✓ done, полировка поз.1 in_progress, остальное queued. Виден на борде (Фацет-колонка появилась). **Удалить после планёрки**: delete production_tasks where order_id=4897; delete b2b_orders id=4897.
- **Группа «Обучение»** в меню производства (`c6edaa8`, проверено Chrome): 📘 Регламент работы (/production-app/guide, кнопка «Открыть учебный заказ» в жёлтом блоке) + 🎓 Учебный заказ ДЕМО-1 (/production-app/demo → находит заказ по custom_number='ДЕМО-1' и редиректит в карточку; если демо удалён — заглушка, ссылка не ломается). Регламент переехал из «Команды» (там остались Идеи). Формулировки без «начальника» — везде «система» (`309f338`).
- **Регламент работы** — страница ВНУТРИ приложения `/production-app/guide` (`d58a7bf`, проверено Chrome): по ролям, на примере ДЕМО-1; в меню Команда → «Регламент работы» + верхняя вкладка «📘 Регламент». (Внешний артефакт-шпаргалка тоже есть: https://claude.ai/code/artifact/e6a8edcb-ec57-47ce-917b-3737512a47a7)

## PROD: ТРИПЛЕКС ПО СЛОЯМ — ЗАВЕРШЕНО И ПРОВЕРЕНО (2026-07-06)
`7c1542e`+`7f1f1b8`, прод. Миграция 20260706_production_tasks_layers.sql применена владельцем вручную (Success), код запушен, сквозной тест пройден.
- Модель: production_tasks += layer (0=изделие: склейка/упаковка; 1..N=стекло пакета; обычные позиции=1), layer_note, UNIQUE +layer.
- buildProductionTasks: триплекс → отдельная цепочка резка→…→закалка на КАЖДОЕ стекло; склейка+упаковка layer 0. launch-production: onConflict +layer; слои НЕЗАВИСИМЫ (обе резки без блокера — параллельно), склейка ← последняя послойная, упаковка ← склейка.
- Станция: материал/толщина по layer задачи; «Готово» закрывает только свой слой. Мои задачи: бейдж «Поз. 1 · Резка · слой 2: 4 мм».
- **ТЕСТ (заказ #4896, 8+4, удалён)**: created=10 = 4 этапа×2 слоя + triplex + packaging; L1 8мм chain #237→240, L2 4мм chain #241→244 (обе резки blockedBy=null), triplex←#244, packaging←triplex. UI станции: партия «Прозрачное М1 4 мм · 1 деталь» появилась. Cleanup: tasks=0, orders=0.

## B2B: ТРИПЛЕКС ИЗ РАЗНЫХ ТОЛЩИН (4+6) + РАСКРОЙ ОБОИХ СТЁКОЛ (2026-07-06)
`4bfc4a3`, прод, проверено Chrome end-to-end.
- calcItem: `triplexExtraGlasses: B2BMaterial[]` — слои 2..N по СВОИМ цене/толщине (материал/закалка/кромка/вес суммой по слоям, продажа = Σ прайса слоёв); склейка за м² изделия ×1. B2BOrderItem += `triplexGlasses[{materialId,materialName,thickness}]`.
- Калькулятор: селекты «Стекло 2»/«Стекло 3» (дефолт «как основное») в форме и модалке; чип «триплекс 8+4».
- Раскрой: station/cutting (партии) и /b2b-cutting (закупка) раскладывают ВСЕ слои пакета в свои партии материал+толщина. Нюанс: «Готово» по любой партии закрывает cutting-задачу всей детали (задача одна на позицию) — v1.
- ПРОВЕРЕНО: 1000×1000, 8мм + Стекло 2=4мм → 9 095 ₽ = 3258+1337+4500 (t8/t4 из glass_price_matrix точь-в-точь), вес 30 кг = (8+4)×2.5, чип «триплекс 8+4».

## B2B: ТРИПЛЕКС В КАЛЬКУЛЯТОРЕ + АВТОРАСЧЁТ ПРОДАЖНОЙ В УСЛУГАХ (2026-07-06)
`e7bdc44`, прод, проверено Chrome end-to-end.
- **calcItem** (lib/b2bCalculator): +hasTriplex/triplexLayers(2|3)/triplexPrice. Пакет N стёкол: материал, закалка, кромка, вес и ПРОДАЖА стекла ×N; триплексация (склейка) за м² ИЗДЕЛИЯ ×1 из справочника услуг (per_m2 «Триплекс» #16: продажа=value 4500, себест=cost_price 2500). B2BOrderItem += triplexLayers/costTriplex/saleTriplex → станция «Триплекс» в маршруте (hasTriplex уже в getApplicableStages).
- **Калькулятор**: чекбокс «Триплекс» (индиго) + селект «2/3 стекла» в форме и модалке редактирования; чип «триплекс N ст.» в позициях; warning если услуги нет. triplexPrice = useMemo по services.
- **ПРОВЕРЕНО живьём**: 1000×1000 Прозрачное М1 8мм закал.+триплекс 2 ст. → 11 016 ₽ = 3258×1×2 + 4500×1 ✓; вес 40 кг (×2) ✓; себест 4 829, маржа 47%.
- **/admin/b2b-services**: для ₽-услуг (fixed/per_m2) форма = Себестоимость → Маржа % → Продажная (авто: продажа = себест/(1−маржа/100), маржа как доля цены — единая логика B2B). Ручная правка продажной сбрасывает маржу; подпись «маржа N% от цены». Проверено: себест 2500 + маржа 40 → 4167 авто (НЕ сохранял — реальная цена 4500 не тронута). margin_override_pct переиспользован для ₽-типов.

## PROD: СТАНЦИИ ФАЦЕТ+ТРИПЛЕКС, ПУЛ ПО ЗАКАЗУ, «СТАНЦИИ» В МЕНЮ (2026-07-06)
`fecd027` + миграция 20260706 (применена Chrome SQL Editor). Проверено Chrome.
- **Модель этапов (lib/productionStages)**: 8 станций. Порядок = физика стекла:
  Резка→Криволинейка→Полировка→Сверление→**Фацет**→Закалка→**Триплекс**→Упаковка.
  getApplicableStages: facet↔hasFacet, triplex↔hasTriplex (кромка/фацет до закалки, триплекс после).
- **Пул на сегодня (/today)**: задачи сгруппированы ПО ЗАКАЗУ (0828-2 · M GLASS · 5 поз.)
  вместо строки-на-позицию; AssignWorker принимает taskIds[] (групповое назначение). Задвоения НЕТ (0, проверено).
- **«Станции» в меню** (sidebar Цех + ProductionTabs) → /production-app/station (индекс→cutting),
  вкладки 8 станций сверху; Резка — партии по материалу+толщине с раскроем (было). «Пул по станциям»→«Пул на сегодня».
- Борд: колонки Фацет/Триплекс (только если есть применимые заказы — visibleCols). Экраны рабочего
  (/p/o, orders/[id]) и /admin/users: этапы/станции += facet, triplex (и curved где не было).
- Миграция 20260706: CHECK stage_key/station += facet,triplex. Текущие 21 заказ без фацета/триплекса → не затронуты.
- Точки касания станций (единый список 8): productionStages, productionRouting(типы), today, station/[station],
  board COLS, p/o BATCH_STAGES, orders/[id] GROUP_ACTIONS, admin/users STATIONS, ProductionTabs, Sidebar.

## PROD: КОНСОЛИДАЦИЯ ЭКРАНОВ /production-app (2026-07-06)
`52369b1`, прод, проверено Chrome.
- Было 6 вкладок в шапке цеха (дублировались в каждой странице своим массивом NAV) + осиротевшая Панель /supervisor. 4 обзорных экрана (Сводка/Борд/Пул/Панель) перекрывались.
- `components/ProductionTabs.tsx` — единая верхняя навигация (client, usePathname): **Обзор · Пул по станциям · Мои задачи · Материал · Документы** (5 вкладок). Вставлена во все экраны production-app (Сводка, board, today, my-queue, material, docs); у today/my-queue навигации раньше не было — добавлена.
- **«Обзор» = 2 вида одних данных**: По срокам (/production-app, бакеты по дедлайну) и Матрица (/production-app/board, заказ×этап). Переключатель «ВИД: По срокам | Матрица» в шапке. Борд больше не отдельная вкладка. Проверено: клик Матрица → /board с тем же тулбаром + «Показать готовые».
- **Панель /supervisor** убрана из всей навигации (осталась по URL — аудит отменённых этапов). Sidebar «Цех»: Сводка→Обзор, «Борд» убран.
- Нюанс: sidebar navItem active() по startsWith — «Обзор» подсвечивается и на подстраницах (косметика, было и раньше).

## PROD: РАЗДЕЛ «ПРОИЗВОДСТВО» — ЧИСТАЯ СТРУКТУРА (2026-07-06)
`8fa49bf`, прод, проверено Chrome. Убрано лишнее из раздела «Производство».
- Было: 3 группы (Заказы: /b2b-orders+/b2b-production+/b2b-cutting; Production App: /production-app+supervisor; Справочники /admin/*). Половина пунктов роль `production` не могла открыть (нет в allowlist), живые экраны /production-app/* были скрыты.
- Стало (Sidebar.tsx, оба рендера — роль production и админ-режим «Произв.»): **ЦЕХ** (Сводка /production-app, Пул на сегодня /today, Борд заказ×этап /board, Мои задачи /my-queue, Заказы в работе /b2b-production) + **МАТЕРИАЛ И ДОКУМЕНТЫ** (Раскрой /b2b-cutting, Материал /production-app/material, Документы /production-app/docs).
- Убрано (по решению владельца): реестр /b2b-orders (остаётся в «Админ») и справочники /admin/* (настройка владельца).
- allowlist роли production (getRole.ts) синхронизирован: убраны мёртвые /b2b-pipeline (воронка продаж) и /production; добавлен /b2b-cutting. Осталось: /, /b2b-production, /b2b-cutting, /production-app, /p/o.
- /production → редирект на /production-app (была дубль /b2b-orders). Header.tsx мёртв (нигде не импортится).
- Внутренняя навигация /production-app (Сводка/Борд/Пул/Мои задачи/Материал/Документы) — своя, работает; sidebar теперь ей соответствует.

## PROD: /b2b-production СИНХРОНИЗИРОВАН + КАРТОЧКА ПЕРЕРАБОТАНА (2026-07-06)
`2493cba`, прод, проверено Chrome.
- Раньше страница читала свой `notes.production_status` (у всех null) → показывала «Ожидает 700». Теперь статус выводится из `notes.stages` (единый источник правды с /b2b-orders): грузим только `launched_at != null`, активные = stages.shipped отсутствует. Результат: **Ожидает 23 (июль) · Отгружено 977**.
- Кнопки статуса пишут в `stages` (cutting→cut, tempering→tempering, ready→packaged, shipped→packaged+shipped, pending→clear) — обе страницы остаются согласованы. `effectiveStatus(stages)` + `itemStage()` (по detail_stages, иначе order-level).
- Пагинация 30/стр (было 20). Крупный №заказа (custom_number||id) в шапке.
- Колонка «Закалка» → «Этап» (текущий этап каждой детали). Примечание = спецработы чипами: подряд/«не мы» (закалка/триплекс/фацет, ⚙ амбер) vs наши (криволинейка/услуги вроде подсветки/пескоструя, синие) + текст comment. Сводка спецработ в шапке заказа. Печатный лист тоже.
- Файл: app/b2b-production/page.tsx (переписан). ⚠️ Если владелец скажет, что закалка у нас своя (не подряд) — переключить её из outsourced в ours.

## PROD: БАЗОВАЯ ОТМЕТКА «ГОТОВО/ОТГРУЖЕНО» ДЛЯ ЗАКАЗОВ ≤ ИЮНЯ 2026 (2026-07-06)
Разовый baseline перед налаживанием цеха: все запущенные заказы ≤ июня помечены готов/упакован/отгружен, июльские оставлены «в работе».
- Скрипт (service-role, scratchpad/baseline_ship.mjs): цель — launched_at ≤ 2026-06-30 и без stages.shipped = **129 заказов**. Заполнена цепочка order-level stages (added_to_group…tempering,packaged,shipped) датой launched_at + записан bulk_action{previous_stages, scope:'baseline_pre_july_2026'} для отката. Также закрыто 8 production_tasks (queued/in_progress/problem → done).
- Реверсивно: бэкап notes 129 строк → scratchpad/backup_baseline_ship_2026-07-06T07-04-*.json + previous_stages в каждом заказе.
- Проверено (БД + Chrome /b2b-orders): ≤ июня НЕ отгружено = 0; осталось «в работе» 23 заказа — все июль 2026 (#4852…#4884, #574). UI: 679 отгружено · 21 в работе, табло «Не начато 21» = июль.
- ⚠️ Прямая правка данных, деплой не нужен (живёт сразу). Только b2b_orders (розница — AmoCRM, не трогаем).

## B2B: СЧЁТ-СПЕЦИФИКАЦИЯ (КП+счёт от ООО) + ПЕРЕВЁРСТКА КП (2026-07-03)
`ec1b7bc`, прод, проверено Chrome на заказе #4883 (ИП Боровлев, 4 поз., 26 164 ₽).
- **Два юрлица:** розница — ИП Мухаморов (НДС 5%, EXECUTOR, /contracts, не тронут); опт B2B — **ООО «МЕТАЛ & ГЛАС МАНУФАКТУР»** (НДС 22%, новая константа `SELLER_B2B` в companyRequisites.ts). ИНН 7716986980, КПП 771601001, ОГРН 1237700622246, р/с 40702810701300039842, Альфа-Банк, БИК 044525593, к/с ...593. ⚠️ В карточке ООО банковский ИНН 7728168971 противоречил — взят 7716986980 (с фактического счёта/ОГРН).
- **«Счёт-спецификация»** = единый документ КП+счёт (счёт-оферта, оплата=акцепт): `/b2b-quotes/[id]/invoice`. Реквизиты продавца ООО + платёжный QR (ГОСТ ST00012, `paymentQrStringFor`), спецификация позиций (Цена/Сумма без скидки/Скидка/Сумма), Итого/в т.ч. НДС 22%/Всего к оплате + сумма прописью (`lib/numToWords.rublesInWords`), юр.текст 5 пунктов, **подпись + гербовая печать ООО** (извлечены из скана 1.jpg, синие чернила кеингом, прозрачный фон → public/seal-ooo.png). Столбец «Сумма» сходится с «Итого» (округление на последнюю строку). ИНН ООО оставлен 7716986980 (по решению владельца).
- **Реквизиты покупателя:** миграция `20260703_b2b_client_requisites.sql` (b2b_clients += full_name/inn/kpp/ogrn/legal_address/bank_account/bank_name/bik/corr_account/supply_contract_no/date). Редактируются на странице счёта + AI-вставка (reuse `/api/ai/parse-customer`), сохраняются в карточку (PATCH `/api/quotes/[id]/invoice-data`, сервис-клиент). Автоподставляются в след. счета.
- **КП переверстан** (`/b2b-quotes/[id]/kp`): акцент-бар, тёмная плашка «Итого к оплате», кнопка «Скачать PDF» (html2canvas+jsPDF), реальные контакты (mglass.pro, +7 925 933 50 33).
- Кнопка **«Счёт»** рядом с «КП» в списке просчётов.
- **TODO owner:** загрузить печать ООО (сейчас только подпись+М.П.); подтвердить банковский ИНН ООО.

## B2B: РЕКОМЕНДАЦИЯ ПО ЦЕНЕ ОТНОСИТЕЛЬНО РЫНКА (2026-07-03)
`b243d06`, прод, проверено Chrome end-to-end на реальном просчёте (4 поз., параллелограмм, 49% маржа).
- Блок «💡 Рекомендация по цене» под средней маржой в B2B-калькуляторе. Сравнивает avg-маржу с target_margin/min_margin из owner-strategy (`useOwnerStrategy`, дефолт 40/25) и подсказывает менеджеру: ниже минимума → поднять; ниже целевой → «можно дороже»; выше цель+12 → «есть запас для торга» (комфортно уступить до target%, крайний предел min%); в коридоре → «В цель». `priceAt(m)=price*(100-avgEm)/(100-m)`.
- Доп. сигналы: худшая позиция (маржа <avg−8) и крупный заказ (>150 кг → заложить логистику, можно взять с меньшей маржой ради объёма).
- Философия по ТЗ владельца: всегда в хорошем плюсе, но не самая дешёвая цена — выигрываем сервисом, не демпингом. Сноска: сравнение с ценами конкурентов добавим по их прайс-листу (Фаза 2).
- Проверено: просчёт 26 164 ₽ (скидка 10%), маржа 49% → «В цель, предел торга ≈17 792 ₽ (25%)» + «Крупный заказ, 220 кг».

## B2B ЧЕРТЁЖ: РАСКРОЙ СКОШЕННЫХ ДЕТАЛЕЙ + ФОЛБЭК ИЗ ФОРМЫ (2026-07-03)
`a63106d`, прод, проверено Chrome end-to-end.
- Скошенные детали (параллелограмм/трапеция/треугольник) режут из ГАБАРИТНОГО прямоугольника (сторона+скос, напр. 1180+615=1795) — расход больше номинала. parse-drawing вернул shape/cut_width_mm/cut_height_mm; калькулятор считает по cut_width×cut_height. Проверено: параллелограмм 1180×870 скос 615 → позиция 1795×870, площадь 2,053→3,123 м², цена 6689→10176 ₽ (×2). Готовый размер + форма — в комментарии.
- Толщина/материал из ФОРМЫ, если на чертеже нет (fThickness/selectedMaterial): эскиз без толщины/материала → Прозрачное М1 8мм из формы.
- Матчер материала улучшен (`4f3a043`): «прозрачное»/не указано → простое прозрачное (не первый рифлёный).
- Панель: «Скошенных деталей: N — раскрой по габариту».

## ДУШЕВАЯ: РЕАЛЬНЫЕ ФОТО МОДЕЛЕЙ berusteklo (2026-07-03)
Владелец забраковал SVG-иллюстрации («стрёмно»). AI-рендеры недоступны (нет GEMINI_API_KEY + google-genai не установлен; у OpenAI billing hard limit). Владелец выбрал: фото berusteklo (внутренний доступ, юр. ок).
- Скриптом (service-role) скачал 12 фото моделей с berusteklo (Tilda CDN, optim webp 746px, имя файла = № модели) и ЗАЛИЛ в наше хранилище Supabase (bucket `shower-models`, M1.webp..M12.webp, public) + upsert в `shower_model_images` (M1–M12). Не хотлинк — перехостинг.
- Калькулятор уже читал админ-фото (modelPhotos) → все 12 моделей теперь показывают реальные фото. Проверено Chrome (zoom): единый стиль, конфигурации совпадают с подписями.
- SVG-набор `ShowerModelIcon` остался как фолбэк (если фото удалить). Правка конкретной модели — заменить фото в /admin/shower-images.

## (устарело — фото поверх) ДУШЕВАЯ: НОВЫЙ ЕДИНЫЙ НАБОР ИЛЛЮСТРАЦИЙ МОДЕЛЕЙ (2026-07-03)
`926b562`, прод, проверено Chrome (zoom). Переписан `components/ShowerModelIcon` — единый фронтальный иллюстрированный стиль всех 12 моделей: стекло с бликом, профиль/рамка, фон-стена+пол, дуга распашной / стрелки раздвижной, у угловых (M4/M6/M7/M8/M9) — боковая возвратная панель в перспективе. Структура конфигураций как у berusteklo, отрисовка своя (только для менеджеров — юр. ок). Активная модель — синяя с подсветкой.
- Приоритет: если в `/admin/shower-images` загружено фото модели — показывается оно (M3 сейчас с фото), иначе новая иллюстрация.

## B2B: РАСПОЗНАВАНИЕ ЧЕРТЕЖА → ПОЗИЦИИ, ФАЗА 1 (2026-07-03)
По рабочему алгоритму (исследование мир.практик → архитектура → внедрение с проверкой). Сохранён алгоритм в память (feedback_workflow_algorithm).
- `/api/ai/parse-drawing` (`03c9767`, прод): Claude читает PDF нативно / фото от руки → детали {размеры мм, толщина, материал, кол-во, ПРОСТЫЕ отверстия vs СЛОЖНЫЕ вырезы, закалка, кромка} + warnings + is_drawing. Отличает габариты детали от штампа/выносок.
- B2B-калькулятор: кнопка «🔍 Распознать чертёж → позиции» у прикреплённого файла → matchDrawingMaterial (толщина+тип) → calcItem → позиции. Панель: добавлено/пропущено, сложность (вырезы=наценка), предупреждения.
- ПРОВЕРЕНО сквозняком (Chrome JS, залогинен): тестовый эскиз (canvas) → 200, Claude разобрал «Полка 600×300 8мм прозрачное закалка 2 отв» + «Дверь 700×1900 8мм бронза 1 ВЫРЕЗ qty2» + умные warnings. Отличил отверстие от выреза верно.
- Research: вырез ($30-50) дороже отверстия ($5-15) кратно; всё до закалки (брак=потеря листа); vision-LLM извлекают размеры без дообучения.
- ФАЗА 2 (ждём прайс-лист конкурента от владельца): операционная себестоимость по тарифу (резка/кромка пог.м/отверстие/вырез-чебурашка/закалка/пескоструй/плёнка) + скидочная логика (прайс без скидок → наша закупка −15/30% → скидка клиенту от объёма) + рекомендации цены (по-B2B, не наглея). Заложить операции как справочник.

## ДУШЕВАЯ БЮДЖЕТ: РЕДИЗАЙН ПО berusteklo — ВСЕ 5 ФАЗ ГОТОВЫ (2026-07-03)
Анализ berusteklo.ru + все фазы сделаны и в проде. Картинки = свои иллюстрации/SVG.
- **Ф1** (`ba4767b`): убран «Быстрый старт».
- **Ф2** (`ba4767b`): все 12 моделей — единый SVG-набор `ShowerModelIcon` (битые jpg убраны); фото из админки `/admin/shower-images` (shower_model_images, GET открыт) если загружено, иначе SVG.
- **Ф3** (`d26f597`): свотчи стекла (CSS-текстуры, 6 типов) и цвета фурнитуры (металлик-градиенты) вместо `<select>`. Константы GLASS_SWATCH/HW_SWATCH в shower/page.tsx.
- **Ф4** (`d26f597`): клиентская карточка (изделие+размеры+бейджи «стекло Nмм закалённое / фурнитура цвет / гарантия 2 года» + «без скидки→скидка→Итого к оплате») + тумблер «Клиентский вид» в шапке (прячет себестоимость/маржу).
- **Ф5** (`3d4e9ca` + миграция ПРИМЕНЕНА через Supabase SQL Editor): таблица `shower_models` (12 строк, сид из кода). Калькулятор читает её (active, по sort) с фолбэком на SHOWER_MODELS. API `/api/admin/shower-models` (GET/POST owner). Админка `/admin/shower-images`→«Модели душевых»: правка названия/описания/базы фурнитуры + вкл/выкл + фото.
- Проверено Chrome: Ф1/Ф2/Ф3/Ф4 вживую; Ф5 — таблица 12 строк в БД (service-role), калькулятор с фолбэком.
- ДАЛЬШЕ (по желанию): свотчи стекла/фурнитуры тоже вынести в БД+админку (сейчас CSS в коде); фотореалистичные фото моделей владелец может грузить в админке — калькулятор их подхватит.

## B2B: РЕДАКТИРОВАНИЕ ПОЗИЦИЙ + УСКОРЕНИЕ ЗАГРУЗКИ (2026-07-03)
`c6deff4`, прод, проверено Chrome:
1. **Редактирование позиций в карточке** (`calculator/b2b`). Модалка правки (материал/толщина/Ш/В/кол-во + пересчёт) уже была, но её кнопки уходили за правый край очень широкой таблицы — их не находили. Сделал всю строку позиции кликабельной → openEdit; в модалку добавил живой пересчёт «Итого позиции»+маржа (calcItem на каждый ввод) и кнопку «Удалить»; подсказка в шапке. Проверено: клик по позиции → форма → кол-во 1→2 → итог 7389→14778 мгновенно.
2. **Долгая загрузка `/b2b-quotes`**. Корень: грузили ВСЕ неархивные заказы (2632, ~2.6 МБ), хотя показываются только незапущенные (6); 2626 запущенных (launched_at) живут в /b2b-orders и в этом списке не видны ни в одной вкладке. Добавил `.is('launched_at', null)` в выборку → грузится ~6 строк, загрузка мгновенная. Возврат в черновик (setStatusDirect→'quote') теперь обнуляет launched_at (колонку+notes), чтобы восстановленные снова попадали в список. Деплой был лишь вторичным фактором.

## B2B: УБРАНО СОГЛАСОВАНИЕ ПРОСЧЁТОВ (2026-07-03)
Владельцу не нужно согласование — везде «Запустить в работу» (`7df6dd3`+`426abea`, прод, проверено Chrome: все 7 активных с кнопкой запуска, вкл. Верин −20% #4881 и согласованный #4861):
- `calculator/b2b`: скидка выше лимита менеджера / просадка ниже мин.цены больше НЕ уводит в `pending_approval` — статус всегда `quote`. Предупреждения переписаны без «согласования».
- `b2b-quotes`: `pending_approval` трактуется как `quote`; кнопка «Запустить в работу» теперь и для `agreed` (у согласованных её не было — зависали). Удалены вкладка «На согласовании», ветки «Согласовать»/«Ожидает», функция approveQuote.
- БД-нюанс: первый скрипт по дефолтному лимиту 1000 строк зацепил 2 СТАРЫХ архивных (id 547/673) вместо видимых 4880/4861 — безвредно (архив). Реальные #4880/#4861 оказались уже `agreed` (кто-то нажал «Согласовать»); их чинит правка кода (agreed→запуск), а не данные. #4880 в архиве — при желании восстановить из Архива.

## ОТКАТ АХА ИЗ B2B-СПРАВОЧНИКА СТЕКЛА (2026-07-03)
ИСПРАВЛЕНИЕ моей ошибки. `/admin/glass-prices` (стекло, cost+sale) — это **B2B производство** (закупка листами за м²), НЕ место для АХА. АХА-себестоимость относится ТОЛЬКО к менеджеру M-Glass. Откат (`8ab893f`, прод, проверено Chrome):
- Исходные значения восстановлены из транскрипта (jsonl, строка 10101) — реальный дамп ДО АХА: Прозрачное cost 355/451/540/781/975 (а не АХА 1300+), sale 1337/1635/1918/3258/3300. Все 18 строк стекла (9 cost + 9 sale) вернул к исходнику. Маржа снова 40–49% (красные минусы были артефактом АХА-инфляции cost).
- Откатил и код, завязанный на АХА-премису: `calculator/shower` price_type 'cost'→**'sale'** (продажная включает закалку; B2B-себестоимость = сырое стекло, душевую по ней нельзя); `calculator/loft` — обратно на продажную (откат отвязки этой сессии).
- Бэкапы: scratchpad/glass_full_before_rollback_*.json (состояние с АХА), glass_sale_backup_* (продажная до подъёма 40%).
- Продажная стекла (40% raise прошлого шага) тоже откатана — она была не нужна, чинила симптом моего же бага.
- ОТКРЫТО: где менеджер M-Glass берёт АХА-себестоимость (mirror/shower/quick) — отдельный вопрос, НЕ в B2B-матрице. Ждёт решения владельца.

## (устарело, откатано) СТЕКЛО: ПРОДАЖНАЯ МАРЖА ≥40% + ЛОФТ ОТВЯЗАН (2026-07-03)
Справочник `/admin/glass-prices` → «Продажная Стекло» (`c85cbfd`, прод, проверено Chrome — все 40.0% зелёным):
- Все 33 приценённые ячейки стекла (glass_price_matrix sale, category=glass) были в МИНУСЕ (продажная ≈ себестоимости после заведения АХА-цен в cost). Подняты до маржи 40% по формуле приложения: цена = (netCost+закалка)/(1−opex11%−40%), netCost = cost×(1+расход)−входной НДС20%. Рост ~2.4×. Пустые/12мм (2 шт) не тронуты. **Бэкап старых значений**: scratchpad/glass_sale_backup_*.json.
- **Отвязал лофт**: `app/calculator/loft/page.tsx` брал ПРОДАЖНУЮ цену стекла как базу себестоимости (и сверху свою маржу) → поднятие продажной взорвало бы лофт. Теперь лофт берёт СЕБЕСТОИМОСТЬ (cost) — цены лофта ~как были. Продажная стекла = цена B2B-клиенту (calculator/b2b, стр.254) и растёт только там.
- Не затронуты: зеркало (своя категория), душевая (берёт cost), быстрый расчёт/зеркало (cost/mirror).
- Орфан: «Сатин тонированный» 5мм = 3427 без себестоимости → маржа не считается (нет cost), не трогал (предсуществующая нестыковка данных).

## ЗАКУПЩИК ВЕРА: +ПРОИЗВОДСТВО (2026-07-03)
Вера (`zakupka@mglass.ru`, role=buyer, permissions.b2b_client_scope=`mglass_only`, see_all_orders=true, manager_code=6). Была: закупщик-секции + B2B M GLASS (менеджер, калькулятор заперт на клиента M GLASS). Добавлено производство (`5ca28c4`, прод):
- `lib/getRole.ts`: scoped-buyer пути += `/production-app`, `/p/o`.
- `app/production-app/layout.tsx`: пускает scoped-buyer (getUserProfile + isMGlassOnlyUser, защита в глубину).
- `app/production-app/supervisor/page.tsx`: панель надзора открыта scoped-buyer, не только owner (было isOwnerRole).
- `components/Sidebar.tsx`: раздел «Производство» (Производство + Панель производства) для scoped-buyer + авто-раскрытие на /production-app.
- `app/b2b-quotes/page.tsx`: buyer в canSeeAll (видит ВСЕ просчёты независимо от see_all_orders; заказы buyer уже видел все).
- Проверено: build ✓; owner-доступ к /production-app/supervisor не сломан (регрессия ок). Сайдбар Веры вживую не снимал — нужна её сессия (пароль не ввожу). На след. загрузке у неё появится «Производство».

## КП-ПЕЧАТЬ: ЧИСТАЯ ВЁРСТКА (2026-07-03)
Правки печатной формы КП (`38362ad`, на проде, проверено вживую на `/kp/10/print` — KP-0174-0):
- Убран блок «КОММЕРЧЕСКОЕ ПРЕДЛОЖЕНИЕ + название изделия» и красный ИТОГО-бокс справа (`.kp-titleblk`/`.kp-totalbox`).
- Убран раздел «ОБЪЕКТ · СПЕЦИФИКАЦИЯ» (`.kp-spec`) — описание теперь только в смете (name+desc+qty+price, `.ds` поддерживает доп.описание).
- Смета перенумерована в **01**; маркетинг-разделы стр.2–3 сдвинуты в 02–08 (без разрыва).
- `.kp-paybar`: фон `var(--red)`, метка «ИТОГО К ОПЛАТЕ» и сумма — белые.
- Файлы: `app/kp/[id]/print/KpDocument.tsx` (JSX), `app/kp/[id]/print/page.tsx` (CSS). Мёртвый CSS `.kp-titleblk/.kp-totalbox/.kp-spec` оставлен (безвреден). build ✓.

## СКОРОСТЬ/НАДЁЖНОСТЬ + UI ПОЛЬЗОВАТЕЛЕЙ (2026-07-02)
Всё на проде, авто-пуш в main работает (пользователь создал `.claude/settings.local.json` с `Bash(git push:*)`; сам файл создать не могу — harness блокирует self-modification).
- **UI /admin/users**: две сворачивающиеся группы **M-Glass / Производство** (свёрнуты по умолчанию); станции рабочего → выпадающий **«Этапы»** (свёрнуто показывает назначенные, клик → переключатели ✓ добавить/убрать). Проверено ВЖИВУЮ на проде. → `app/admin/users/page.tsx`
- **Батч 1** (`8dacfab`): `app/error.tsx` + `app/global-error.tsx` (нет белых экранов; Next 16 проп ретрая = `unstable_retry`, в кнопке `location.reload()`); try/catch/finally + «Повторить» в 6 загрузчиках (admin/dashboard, manager-dashboard, cfo/margins, cfo/unit, admin/users, calculator/shower).
- **Батч 2** (`e48bb65`): ещё ~40 загрузчиков — `.catch(() => setLoading(false))` (паттерн из my-earnings) по калькуляторам/аналитике/цеху/админ-конфигам/marketing; loft — полный try/finally. Вечный спиннер устранён.
- **Индексы** (`d1d3bf2` + ПРИМЕНЕНЫ в прод через SQL Editor): idx b2b_orders.created_at, b2b_orders(created_by,created_at), calculations(created_by,created_at). «Success. No rows returned».
- **Ложный бейдж** (`ac23e32`): убран триггер «Похоже, это уже заказ» по client_order_number (референс клиента, не признак заказа).
- НЕ трогал: b2b_orders payload (items нужен для прогресса, сузить нельзя — глубокая оптимизация = lazy-load по годам, отдельным аккуратным проходом с замерами). Preview-деплои Vercel красные из-за env-scope (только Production) — прод не затронут.
- Предсуществующее: 31 TS-ошибка в `__tests__/calculators/mirror.test.ts` (сигнатуры), next build их не типизирует.

## МИГРАЦИЯ КАБИНЕТА ПРИМЕНЕНА (2026-07-02)
- 20260702_partner_cabinet.sql ПРИМЕНЕНА в прод через Supabase SQL Editor (Chrome MCP; MCP-токена нет, DDL иначе не идёт). Проверено: b2b_clients.user_id ✅, b2b_orders.source ✅, RLS «Partner reads own orders»/«own client» ✅, non-internal заказов 0.
- Функцию auth.is_partner() НЕ создаём (SQL-редактор не даёт объекты в auth, ERROR 42501; не нужна — политики используют auth.uid() напрямую). Файл миграции приведён в соответствие.
- Токен Supabase НЕ брал/не хранил (секрет). DDL применил через уже залогиненный SQL Editor.
- ГОТОВО к достройке кода кабинета: колонки есть. Осталось (за владельцем): завести тестового партнёра (auth-учётка + users.role=partner + b2b_clients.user_id + отдельная org для изоляции). После — строю калькулятор/историю/заявку/детали и верифицирую на живом партнёре.

## РОЛЕВЫЕ ПАНЕЛИ + внедрение (2026-07-02)
4 ролевые аналитические панели (Solution Architect, Security/DBA, Product/UX, Ops) по каждому вопросу перед внедрением. Внедрено безопасное:
- **KP-страница безопасность**: `/b2b-quotes/[id]/kp` больше не читает b2b_orders анон-ключом. Новый `GET /api/quotes/[id]/kp-data` с проверкой доступа (owner/see_all/created_by/партнёр), страница берёт данные оттуда. Закрыт горизонтальный доступ между внутренними.
- **Калькулятор `?orderId=` → true UPDATE**: редактирование той же строки (не дубль!). Грузит custom_number/client_order_number/notes; handleSave ветвит insert/update; при update — MERGE notes (сохраняем status/history/оплату/launched_at), created_by не трогаем, пишем updated_by. Кнопка «Обновить просчёт», подпись «Редактируется №…». Иконка 🧮 → «Редактировать» (копия — соседняя ⧉).
- **Материал-гейт единый** (lib/materialGate.ts, isCuttingBlocked): резку нельзя закрыть без материала из ОБЕИХ серверных точек (/api/production-tasks + sync-stages), не только из UI станции. Обход body.force:true. 409 material_not_arrived.
- **Ops**: экран «Пул на сегодня» → «Пул по станциям» (в соответствие с табами; дневного планирования нет — не внедряем, за владельцем).
- Проверка: tsc/build зелёные.

### SECURITY/DBA нашёл важное (проверено на живой БД):
- Bucket b2b-attachments УЖЕ приватный (public.sql НЕ применён) → публичного лика нет. НО код сохраняет getPublicUrl → ссылки на вложения СЕЙЧАС БИТЫЕ (400). РЕАЛЬНЫЙ БАГ — чинить: хранить path + signed URL на сервере (route + reader). Следующий шаг.
- b2b_clients.user_id и partner-RLS НЕ применены.

### КАБИНЕТ ПАРТНЁРА — архитектура+security готовы, ждём решений владельца:
- Модель: b2b_orders.source ('internal'|'partner') + статусы partner_draft/partner_submitted; расчёт всегда серверный + whitelist sanitizeQuote (нет утечки cost/margin); партнёр к данным только через service-role API. Фазы: P0 фундамент → P1a калькулятор+история → P1b заявка с PDF → P2 realtime(polling).
- Готовый SQL (Security/DBA): auth.is_partner(); partner-SELECT политики b2b_orders/b2b_clients по client_id/user_id=auth.uid(); storage-RLS по префиксу order_id + signed URLs. КЛЮЧ: партнёра НЕ класть в profiles org=1 (иначе org FOR ALL политика течёт) — отдельная org на партнёра.
- ЗА ВЛАДЕЛЬЦЕМ: завести учётки партнёров + b2b_clients.user_id; отдельная org для изоляции; замена storage-политик (вместе с правкой кода); бизнес-правило partner_submitted→внутренний трек.
- БЕЗОПАСНО В КОДЕ СЕЙЧАС (можно применить миграции через Supabase MCP): user_id колонка, partner-RLS (no-op для внутренних), весь серверный код кабинета, партнёрский калькулятор/история/заявка. Активируется после заведения партнёров.

## КОМПЛЕКСНЫЙ АУДИТ B2B + улучшения (2026-07-02)
4 параллельных аудита (просчёт-флоу, производство, аналитика/данные, кабинет партнёра). Реализованы безопасные высокоценные правки:
- **Prod «третье зеркало»** (lib/productionOrderMirror.ts): закрытие всех позиций этапа в цехе → проставляет order-level notes.stages (cut/edge_processed/drilled/tempering/packaged), вызывается из /api/production-tasks/[id] и sync-stages. Теперь /b2b-orders и Сводка отражают цех. Только продвигает (не затирает ручное).
- **Борд**: понимает ОБЕ конвенции ключей (edge||edge_processed, packed||packaged — импорт vs /b2b-orders) + учитывает docs_printed для «Чертёж». docs-printed route теперь пишет и stages.printed (отметка Валерии видна).
- **Дедлайн**: deadline_date теперь в приоритете в /b2b-orders (getPlannedReadyDate) и supervisor (getPlannedDeadline) — раньше игнорировали явный срок.
- **Просчёты**: PDF-роут итог = total_after_discount||total_sale_inc_vat (не «0 ₽»); калькулятор блокирует «Сохранить» после сохранения + сброс при правке позиций (защита от дублей); looksLikeOrder фикс приоритета (?? vs >); duplicateQuote пишет created_by_name+manager_name текущего; saveDiscount пишет updated_by мету.
- **Аналитика /cfo/b2b**: средний чек по заказам с суммой (rev>0), не занижается 15 нулевыми.
- НДС 22%, скидки импорта (суммы уже финальные) — НЕ трогал (корректно).

### ОТМЕЧЕНО ВЛАДЕЛЬЦУ (не делал — нужны решения/действия):
- **Кабинет партнёра** (готов ~30%): применить миграцию 20260701_b2b_client_portal + завести учётки партнёров + `b2b_clients.user_id`. RLS-изоляция: org-level `FOR ALL` политика даёт доступ ко всему оргу; partner-SELECT-политика НЕ изолирует пока org-политика не переработана (архитектурное решение). Пока изоляция на service-role+client_id в partner-API. Приватность bucket b2b-attachments (сейчас public) — для внешних партнёров решить (signed URLs). Дальше в коде безопасно: партнёрский калькулятор (серверный расчёт, только клиентская цена), история просчётов (status partner_quote), заявка с PDF, детализация+polling статус.
- **KP-страница** /b2b-quotes/[id]/kp — client-side без проверки доступа (полагается на RLS). Усилить серверной проверкой (как в PDF-роуте). [решение по RLS b2b_orders]
- **Калькулятор ?orderId=** — «редактирование» создаёт дубль с новым автором, теряет №/статус. Сделать true UPDATE или переименовать «Дублировать».
- **Prod**: production_day не заполняется (нужно ли дневное планирование?); материал-гейт только на резке (вынести в /api/production-tasks); мёртвые экраны /b2b-production, /b2b-pipeline, /production (подтвердить удаление); supervisor не в навигации роли production.

## ИСПРАВЛЕНИЕ: «Архив расчётов» = /admin/archive (2026-07-02)
- Владелец: «Архив расчётов B2B как будто ничего не поменялось». Причина: в сайдбаре пункт «Архив расчётов» → `/admin/archive` (а не `/b2b-quotes` «B2B Просчёты», куда я сначала по ошибке добавил фичи).
- ОТКАТ: `app/b2b-quotes/page.tsx` возвращён к состоянию до архивных правок (`git checkout 00456c2 -- ...`) — «Просчёты» снова обычный операционный список. Калькулятор без PDF ОСТАЁТСЯ (другой файл).
- ПЕРЕСТРОЕН `/admin/archive` в полноценный «Архив расчётов B2B»: реальные просчёты людей (created_by IS NOT NULL — импорт исключён), группировка по месяцам (новые сверху, текущий раскрыт, сворачиваемо), колонки № заказа/клиент/кто считал/статус/дата/сумма, фильтры (поиск/период/автор/клиент), вкладки Архив/Корзина. Удаление админом (admin/ceo): чекбокс+«Удалить выбранные», «Удалить месяц» → мягко в Корзину (archived_at); в Корзине — Восстановить / Удалить навсегда (hard delete). PDF/КП по строке. tsc/build зелёные.
- Данных сейчас: 9 реальных просчётов (июль, Айжан).

## (устарело) АРХИВ на /b2b-quotes — откачено, см. выше
- Калькулятор /calculator/b2b: УБРАНА прямая генерация PDF (кнопка «Скачать КП (PDF)» в блоке «сохранено»). Теперь после сохранения — только переход в «Просчёты», PDF копируется оттуда. Цель: вся история просчётов каждого человека фиксируется (сохранение обязательно).
- /b2b-quotes стал АРХИВОМ просчётов:
  - Группировка по МЕСЯЦАМ (новые сверху), каждый месяц — сворачиваемый; новейший раскрыт по умолчанию. Заголовок месяца: N просч. + сумма.
  - Колонки уже были: № заказа (custom_number), клиент, «Просчитал: <автор>».
  - ФИЛЬТРЫ: период (дата от/до), кто считал (селект authorOptions), клиент (селект clientOptions) + сброс.
  - УДАЛЕНИЕ АДМИНОМ (admin/ceo): чекбокс на строке + «Удалить выбранные»; «Выбрать всё»/«Удалить месяц» в шапке месяца. Soft-delete (archived_at) → восстановимо из /admin/archive. Функции archiveIds/deleteSelected/deleteMonth.
  - ВАЖНО: импорт из таблицы (created_by_name ~ /импорт/i, 2606 шт) ИСКЛЮЧЁН из архива — показываем только реальные просчёты людей. Сейчас их 9 (июль, автор Айжан: 3 quote + 6 sent).
  - Вкладка «Все» теперь = вся история (включая запущенные в работу), а не только не-запущенные — чтобы видеть полную историю человека. counts тоже без импорта.
- Проверка: tsc/build зелёные. Предсуществующие eslint-ошибки (setState-in-effect в loadQuotes/калькуляторе) — не мои, билд не блокируют.

## ФИКС /b2b-orders — июль не виден + дерево год→месяц (2026-07-01)
- ПРИЧИНА (мой регресс от импорта истории): страница брала `.limit(1000)` + `.order(created_at ASC)` → мои 1927 старых заказов 2024/2025 забили выборку, 2026/июль не доходили. Плюс у истории не было `launched_at` → «Без даты запуска».
- Диагноз на данных: всего 2612 (не quote, не архив); июльских заказов 6 (все с launched_at, девочки всё сделали верно); запуски 2026: янв74/фев88/мар117/апр131/май113/июн156/июл6.
- ФИКС ДАННЫХ: проставлен `launched_at = created_at` для 1927 исторических (батчами по created_at; 17 значений). Осталось без launched_at: 0.
- ФИКС КОДА (app/b2b-orders/page.tsx): (1) выборка теперь ПОСТРАНИЧНАЯ (fetchAllOrders, order DESC) — тянет все 2612; (2) месяцы сортируются новые→старые; (3) добавлен уровень ГОД (yearGroups, expandedYears/toggleYear) — год→месяц→заказы; (4) по умолчанию раскрыт ТОЛЬКО текущий месяц (и его год), остальное свёрнуто (правка по просьбе владельца). tsc/build зелёные (2 eslint-warn предсуществующие).
- РЕШЕНИЕ по историческим в /b2b-orders: исключены из выборки страницы по метке notes.historical:true (коммит 0001bea) — грузить 2612 строк было тяжело, и операционному списку история 2024/2025 не нужна (она в аналитике /cfo/b2b). Страница грузит 685 реальных заказов 2026. Проверено данными: июль 6, месяцы июн156/май113/апр131/мар117/фев88/янв74, без-даты 0.
- Chrome MCP не снимает скриншот /b2b-orders: страница SPA не даёт document_idle (расширение ждёт его 45с и падает) — так же было до правок. Визуально не подтверждено, верификация по коду(build)+данным(скрипты). Владельцу: перезагрузить /b2b-orders, ждём «2026 год» → месяцы (июль сверху, последние 3 раскрыты).

## РЕШЕНИЯ ВЛАДЕЛЬЦА (2026-07-01)
- **НДС B2B = 22%, ВЕРНО.** Клиент должен его видеть, он есть в КП (`/b2b-quotes/[id]/kp`). НЕ менять. Вопрос закрыт.
- **Всё до конца июня НЕ трогаем** (исторические заказы как есть).
- **С июля — правильный поток:** B2B-заказ рождается из истории просчётов B2B (калькулятор `/calculator/b2b` сохраняет items → `/b2b-quotes` → «Запустить в работу»). У июльских заказов будут items = полная детализация по позициям. Поток проверен без пробелов: confirmWorkDate (b2b-quotes:398) пишет launched_at/deadline_date + зовёт launch-production, который читает order.items и строит позиционные production_tasks (buildProductionTasks, идемпотентный upsert). Борд/аналитика подхватывают детали автоматически.
- Правка борда: готов-к-отгрузке теперь = packed-ячейка done (order-level флаг ИЛИ все позиции упакованы по detail_stages) — чтобы новые июльские (item-level) заказы корректно уходили в «готов».

## B2B — дорожная карта (4 пункта, порядок владельца)
Многоракурсный аудит B2B выполнен (3 параллельных агента: данные/аналитика, поток КП→клиент, производство/закупки). Порядок работ: **1) B2B-аналитика → 2) починить рассинхрон производства → 3) борд «заказ×этапы» → 4) кабинет партнёра.**

### Импорт истории B2B 2024/2025 — СДЕЛАНО (2026-07-01)
- Источник: та же Google-таблица B2B (id 13k-jejg0vUf6DCL_Ktd_gKn9Pvs3iR2W), скачана как xlsx (все вкладки, включая СКРЫТЫЕ — там 2024 и H1/часть 2025). Помесячные вкладки.
- ВАЖНО: разметка столбцов менялась внутри 2025 (Янв–Май: оборотка col4; Июнь: col6; Июль–Дек + весь 2026: col7). Парсер детектит столбец оборотки ПО ЗАГОЛОВКУ «Начислено с НДС» — иначе H2 2025 терялся. Номер заказа всегда col2, месяц — из названия вкладки. Сверка: парс 2026 = 15.81 млн ≈ база 15.86 млн (99.7%, метод валиден).
- Вставлено 1927 заказов через service-role: **2024 (авг–дек) = 480 заказов, 13 213 807 ₽; 2025 (весь год) = 1447 заказов, 36 436 584 ₽.** items нет (истории по позициям в таблице нет). notes.status=confirmed, notes.stages.shipped=true (чтобы НЕ попадали в цеховой борд/Сводку — проверено: активных осталось 72), historical:true.
- Теги source: `sheet_import_2024`, `sheet_import_2025` — для отката (`DELETE ... WHERE notes ILIKE '%"source":"sheet_import_2024"%'`).
- Аналитика `/cfo/b2b` теперь показывает 3 года: 2024 (13.2М, частичный), 2025 (36.4М, полный), 2026 (15.86М). Код не менялся — годовой разрез уже был, годы строятся из данных.
- H1 2024 (янв–июль) в таблице НЕТ — история начинается с августа 2024.

### #1 B2B-аналитика — СДЕЛАНО (+годовой разрез)
- ГОДОВОЙ РАЗРЕЗ (2026-07-01): фильтр переделан со скользящих периодов на КАЛЕНДАРНЫЕ ГОДА. Блок «Оборот по годам» (карточка на каждый год: оборотка+заказов+чек, YoY-обзор) + селектор «Все годы / 2026 / …» + помесячно внутри выбранного года + топ клиентов. Годы строятся динамически из данных — 2025/2024 появятся сами после импорта.
- ПОЛНОТА ДАННЫХ (ответ владельцу): в системе только 2026 B2B (679 заказов, 15.86 млн — перенесён полностью). 2024 и 2025 B2B НЕ заносили (решение «сначала только 2026»), НЕ потеряны. `fetched_sheet.csv` в корне — это ОБЩИЙ маркетингово-продажный дашборд компании (ИТОГО оплаты, бюджет, заявки, каналы, B2C+B2B вместе), НЕ B2B-список заказов → из него B2B-по-годам чисто не достать. Для 2024/2025 нужен B2B-список заказов тех лет (та же таблица-источник, откуда брали 2026, если в ней есть прошлые годы). ОЖИДАЕТ подтверждения владельца.

- БАГ И ФИКС (2026-07-01): страница `/cfo/b2b` НЕ фильтровала archived_at → суммировала ВСЕ прогоны импорта (sheet_import_2026 + _v2 + _v3 + старые «без source») → оборотка 59 млн вместо реальных 15.86 млн. Добавлен `.is('archived_at', null)` в fetchAll. Теперь: 679 заказов (только v3), оборотка 15 859 110 ₽ = отчёт владельца до рубля. Помесячно: янв 1.78 / фев 1.77 / мар 3.28 / апр 2.87 / май 2.98 / июн 3.18 млн.
- У текущих 679 заказов items НЕТ (импорт) → блок «материал/закалка» пуст (честное сообщение «наполнится с июльских заказов из калькулятора»). 171 заказ с items, что был виден без фильтра — это архивные/старые.

- Новая вкладка `/cfo/b2b` (серверный компонент на service-role, минует RLS, постранично тянет все 2672 заказа). Ссылки добавлены в шапку и быстрые ссылки `/cfo`.
- Оборотка = Σ total_after_discount по реальным заказам (не quote/pending_approval). Разбивка себестоимости (материал=Σitems[].costMaterial, закалка=ΣcostTempering, прочее=costWithVat−mat−temp) + валовая прибыль + маржа. Блоки: Оборот / Разбивка / По месяцам 2026 / Топ клиентов. Фильтр периода (месяц/квартал/полгода/год/всё).
- **КЛЮЧЕВОЙ нюанс (проверено на живой БД):** только **334 из 2672** заказов имеют `items` (сделаны через калькулятор). 2338 — импорт из таблицы без разбивки. Поэтому материал/закалка честно показываются ОТДЕЛЬНО с явным покрытием («N из M заказов, X% оборота») + amber-баннер, чтобы доли не вводили в заблуждение. Оборотка при этом полная по всем.
- Данные для аналитики уже лежат в БД — миграций НЕ потребовалось, только чтение. Впредь покрытие растёт (новые заказы из калькулятора имеют items).
- Проверка: tsc/eslint/build ЗЕЛЁНЫЕ. Логика сверена диагностикой на живых данных (оборотка реальных ~19 млн ₽, материал/закалка сходятся).

### #3 Рассинхрон производства — СДЕЛАНО
- #3a Обратное зеркало: новый POST /api/b2b-orders/[id]/sync-stages (service-role) — отметка/отмена этапа с orders/[id] (persistStageUpdate done + unsetStage) и /p/o (markSelectedStages done) теперь зеркалится в production_tasks. Best-effort, псевдоэтап 'problem' пропускается, исторические заказы без задач — no-op. Проверено: WHERE (order_id,item_index,stage_key) матчит ровно 1 строку на живых 8 задачах.
- #3b Криволинейка оживлена: чекбокс «Криволинейка» в калькуляторе b2b (add+edit) → item.shape='curved'; curved добавлен в GROUP_ACTIONS orders/[id] с фильтром по shape==='curved'. Маршрут уже умел curved (getApplicableStages), CHECK-констрейнт +curved применён ранее.
- #3c Кнопка «Взял в работу» в my-queue (action:'start') → статус in_progress, индикатор «🔧 в работе». WIP стал видимым.
- #3d Размеры детали на карточках my-queue: грузим items заказа, показываем Ш×В · материал толщина · кол-во.
- Проверка: tsc/eslint(без новых ошибок)/build ЗЕЛЁНЫЕ. production_tasks = 8 строк (1 тестовый заказ #2816).

### #2 Борд начальника «заказ × этапы» — СДЕЛАНО
- Новый экран `/production-app/board` (серверный, service-role), доступен роли production (не только admin/ceo как старый /supervisor). В навигации Сводки.
- Строки = активные заказы (не отгружены), колонки = лента: Чертёж/Материал/Резка/Полировка/Сверление/Закалка/Упаковка. Ячейки: готово✓/в работе(имя)/частично(n/m)/проблема⚠/не начато. Синяя рамка = текущий этап.
- Основа — notes.stages (order-level флаги printed/material_ordered/cut/edge/drilled/tempering/packed/shipped, данные по ВСЕМ 72 активным). Уточнение (в работе/частично/проблема) — из detail_stages+production_tasks нового цеха.
- Готов = packed=true (как Сводка). Фильтр «показать/скрыть готовые». Проверено: 72 активных → 68 в работе / 4 готовы. tsc/build зелёные.
- На будущее: старый /production-app/supervisor можно свести к борду.

### #4 Кабинет партнёра — ПЕРВЫЙ СРЕЗ СДЕЛАН (фундамент, доступ никому не выдан)
- Роль `partner` в lib/getRole.ts (Role, isRole, ROLE_ALLOWED.partner=['/', '/partner']). Внешний партнёр видит только свой кабинет.
- Миграция `supabase/migrations/20260701_b2b_client_portal.sql`: b2b_clients.user_id uuid → auth.users (аддитивно). **НЕ ПРИМЕНЕНА** (Supabase MCP Unauthorized, авто-раннера нет). Портал работает и без неё: API ловит ошибку отсутствия колонки → «не привязан».
- `/partner/layout.tsx` — гард (partner/owner). `/partner/page.tsx` — read-only «Мои заказы»: этап (из notes.stages ленты), % готовности, срок, цена (total_after_discount). Никакой себестоимости/маржи.
- `/api/partner/orders` — scoped СТРОГО по b2b_clients.user_id=auth.uid(); чужие данные не отдаёт; не привязан/нет колонки → {linked:false}.
- app/page.tsx: партнёр редиректится с '/' на '/partner' (не видит панель менеджера).
- Проверка: tsc/eslint/build зелёные. b2b_clients=48, колонки user_id пока нет (портал корректно показывает «не привязан»).
- **ШАГИ ВЛАДЕЛЬЦА для активации (внешний доступ — его решение):** 1) применить миграцию в Supabase SQL Editor; 2) завести партнёру auth-учётку + role='partner' в public.users; 3) проставить b2b_clients.user_id нужному клиенту.
- ДАЛЬШЕ по #4 (следующие срезы): облегчённый калькулятор партнёра (только цена+его скидка) → история его просчётов → заявка в работу с PDF-чертежом → статус в realtime → (будущее) ИИ-сверка чертежа с просчётом. Для realtime статус лучше вынести из notes.status в колонку.
- Открытый вопрос: НДС в HTML-КП (/b2b-quotes/[id]/kp) = 22% (канон проекта 12%) — свериться для B2B.
- Открытый вопрос к владельцу: НДС в HTML-КП (`/b2b-quotes/[id]/kp`) = 22% (канонический налог проекта 12%) — свериться, что правильно для B2B.

## B2B-производство — фаза «команда цеха» (СДЕЛАНО, деплой)
- Миграция (применена): users.production_stations[] (мультистанции), production_tasks CHECK +curved.
- 6 учёток production: Бекмурза[cutting,curved], Эльзат[polishing], Адилет[drilling], Никита[tempering,packaging], Сергей[], Валерия[]. Пароли *Cex26 (в /admin/users). Триггер auth→users авто-создаёт строку — заводить через update, не insert.
- Этап «Криволинейка» (curved) — маршрут при item.shape==='curved'. Фильтры my-queue/today/station/AssignWorker — на production_stations[].
- /admin/users: мультивыбор станций (чипы) + фильтр по роли/отделу «Отдел» (ad1c4e5).
- /production-app (Сводка): группировка по сроку выдачи (готовы/просрочено/сегодня/завтра/послезавтра/позже)+готовность+нав. /material (Сергей, read-only). /docs (Валерия, печать+отметка notes.docs_printed). API /api/b2b-orders/[id]/docs-printed.
- Срок сдачи: захват «Срок сдачи» (дефолт +14д) в «Запустить в работу» → notes.deadline_date; Сводка группирует по нему (ad1c4e5). ОПЦИОНАЛЬНО на будущее: импорт «Ориентировочной даты» из Google-таблицы для исторических активных заказов.
Коммиты: 497c079 (мультистанции+curved), 03f1ce6 (Сводка+Материал+Документы), ad1c4e5 (срок сдачи + фильтр отдела).

## B2B-производство — контур (автономная достройка)
СДЕЛАНО и задеплоено: ядро (миграция production_tasks, авто-маршрут, генерация при запуске, «Пул на сегодня», ролевая защита /p/o), фикс даты «В работу», объединение в одну кнопку «Запустить в работу». Worker-модуль (коммит 525f8d5): A1 станция рабочего в /admin/users; A2 назначение задач рабочим в «Пуле на сегодня» (AssignWorker); A3 my-queue рабочий; B cutover — /api/production-tasks/[id] пишет и в production_tasks, и в notes.detail_stages.
СДЕЛАНО: C — печатная «Заявка поставщику» из раскроя (4597662). D — агрегированный вид резчика /production-app/cutting (партии материал+толщина из всех заказов, отметка партией/строкой). E — PWA цеха (production-app.webmanifest start_url /production-app, sw.js, RegisterSW). Коммит 5c9c389.
СДЕЛАНО (продолжение по порядку): агрегация ВСЕХ станций /production-app/station/[station] (резка с раскроем, остальные — детали/площадь, только готовые задачи), /cutting → редирект, «Мои задачи» ведёт на станцию рабочего (d530f7b). Учёт закупок: процурмент-система /admin/procurement УЖЕ существует (статусы счёт→оплата→забрали→закрыто + платежи) — добавил связку «Сохранить в закупки» из раскроя (090b767), миграция не понадобилась.
СДЕЛАНО (по порядку): гейт резки по приходу материала — резка строки блокируется, если по заказу есть открытая заявка в закупках, не «забрана» (da2410b); ₽-оценка себестоимости в раскрое по канонической формуле sheetCost (3bd1a49); сверка со складом — миграция b2b_materials.stock_sheets ПРИМЕНЕНА в прод, поле в /admin/b2b-materials, колонка «Склад/докупить» в раскрое (8a6bb07).
Весь контур B2B-производства реализован и в проде. Операционный пред-шаг (за владельцем): завести рабочих production + станции в /admin/users, вести остатки склада в /admin/b2b-materials.
Открытое: нет production-пользователей в БД (роли manager 5/admin 1/buyer 1) — владелец заводит рабочих сам через /admin/users; my-queue визуально не проверен (некого залогинить).

## Текущая задача
Production-очереди цеха (план одобрен). Код написан, typecheck/eslint/build ЗЕЛЁНЫЕ. Миграция ПРИМЕНЕНА к проду (через Supabase SQL Editor в браузере, подтверждено service-role скриптом: production_tasks ЕСТЬ 0 строк, users.production_station ЕСТЬ). Осталось: коммит + push (Vercel auto-deploy), затем Фаза 1 — тестовый запуск реального B2B-заказа «в работу» → проверить, что production_tasks наполнилась; regression — 679 заказов 2026 не изменились. Экраны my-queue/today читают новую таблицу (Фаза 2), запись этапов пока через старый /p/o (notes.detail_stages) — cutover позже.

## Готово (производственные очереди) — код написан, миграция применена
- supabase/migrations/20260701_production_tasks.sql — таблица production_tasks + users.production_station (НЕ применена)
- lib/b2bCalculator.ts — hasHoles/shape/hasTriplex в B2BOrderItem; app/calculator/b2b/page.tsx — чекбокс «Сверловка» (add+edit режимы)
- lib/productionStages.ts — getApplicableStages учитывает hasHoles (!== false, обратная совместимость)
- lib/productionRouting.ts (новый) — buildItemRoute/buildProductionTasks/ANDON_REASONS
- app/api/b2b-orders/[id]/launch-production/route.ts (новый) — генерация задач, идемпотентно
- app/b2b-quotes/page.tsx handleConfirm — best-effort вызов launch-production при isLaunched
- app/production-app/my-queue/page.tsx (новый) — личная очередь рабочего, секции Готово/Ожидаю, действия Выполнено/Проблема
- app/production-app/today/page.tsx (новый) — пул цеха по станциям + активные андон-проблемы, для супервайзера
- app/p/o/layout.tsx (новый) — ролевая защита (production/owner), раньше /p/o/[orderId] был открыт без проверки роли

## Прошлая задача (для справки)
Импорт 2026 из Google-таблицы ВЫПОЛНЕН и задеплоен (коммит 1be220f). Следующее после production-очередей — улучшение flow «калькулятор→просчёт→в работу» (3 разрыва Слоя A, см. ниже) к cutover 2026-07-01.

## Импорт 2026 — v3, ФИНАЛ (rebuild из таблицы)
- v1 (баг: regex номера терял июньский формат 00XXX) и v2 (баг: openpyxl bool False читался как True через str()) — оба архивированы, не удалены.
- v3 source='sheet_import_2026_v3': 679 активных заказов. Январь–май = принудительно confirmed (явное решение владельца — "должны быть выполнены"). Июнь = по факту чекбоксов с каскадом (если shipped→всё true): 81 confirmed (отгружен) / 75 sent (в работе).
- Сумма 15 859 109,9 ₽ = отчёт владельца 15 859 110 ₽ (сходится до рубля, проверено).
- Менеджер из колонки C сохранён в notes.manager_name.
- Следующий шаг UI (текущий): визуально различать отгружен/не отгружен на карточках заказа в /app/b2b-orders/page.tsx + цветовой индикатор (зелёный/красный) в шапке страницы между счётчиком и кнопкой "Производственный день". Отсутствие client_id НЕ должно влиять на этот индикатор — это независимый сигнал (client_unverified в notes).

## Импорт 2026 — СДЕЛАНО (rebuild из таблицы) [устарело, см. v3 выше]
- Решение владельца: таблица = источник истины, позиции (`items`) теряем осознанно (в БД было лишь 45% с items), rebuild одобрен.
- Перенесено **579 заказов** из листа в `b2b_orders` (custom_number, client_id/скидка, launched_at, total_sale_inc_vat, notes.status=confirmed/sent, notes.stages, source='sheet_import_2026'). 107 мусорных итоговых строк листа отброшены.
- Исходные **733 архивированы** (`archived_at`, НЕ удалены). Бэкап: `scratchpad/b2b_orders_backup.json` (733) + `b2b_clients_backup.json`.
- 10 заказов без клиента — discount 10% по умолчанию (`notes.discount_defaulted`), 7 клиентов на сверку (Борис Воронеж, Константин, Егор Воронеж, Стеклянная Роскошь(Воронеж), Александр Емельянов, Эльхан, Евгений от Алексая).
- Активных заказов: 579. notes — text-колонка с JSON. organization_id=1.
- ОТКАТ если надо: восстановить из b2b_orders_backup.json + снять archived_at со старых / удалить импортированные (source='sheet_import_2026').

## Flow B2B — Слой A
- ✅ Фикс #1: запущенные (sent/confirmed) больше НЕ показываются в «Просчётах» (`app/b2b-quotes/page.tsx`: notLaunched-фильтр, убран таб «В работе», 'all'→'Активные').
- ✅ Фикс #2: таб «Сегодня» = просчитано сегодня и ещё не запущено (isToday по created_at); счётчики all/today; подзаголовок «N активных · M сегодня». Бонус: убран антипаттерн setState-в-useMemo (сброс страницы → в обработчиках вкладки/поиска).
- Проверка: typecheck app — чисто; eslint моих правок — чисто (осталась 1 пред-существующая ошибка на useEffect loadQuotes:514, не моя).

## Следующий шаг — Слой A, фикс #3
Редактирование заказа: сейчас правка в калькуляторе делает новый insert (`app/calculator/b2b/page.tsx:~281,681`). Нужно: update того же ряда + снапшот «было» в notes.change_log[], итоги считать по «стало». Затем — Слой B (материалы/раскрой→закупка, маршрут с ветвлением, подрядчики).

## B2B — что выяснено (grounded, эта сессия)
- `b2b_orders` в проде: **733 строки, ВСЕ created_at=2026**. История 2024–2025 в БД отсутствует (есть только в Google-таблице, ~2700 заказов, 2026 ~691).
- Причина «заказы 2026 вылетели»: **542/733 (74%) без launched_at, 401 без status в notes** → не попадают в помесячную группировку (`b2b-orders` группирует по launch-месяцу) → висят в «без даты»/свёрнутых месяцах. Таймзонный сдвиг (гипотеза H1) ОПРОВЕРГНУТ: дат «31.12» = 0.
- Просчёты и заказы — одна таблица `b2b_orders`; статус в JSON `notes.status` (quote/sent/agreed/confirmed/pending_approval). Колонка `status` в схеме не используется.
- Slой A разрывы: (1) `b2b-quotes` грузит всю таблицу без фильтра → запущенные не исчезают из просчётов; (2) нет среза «сегодня/не запущено»; (3) редактирование = новый insert, без истории «было/стало».
- Slой B: раскрой (`cuttingOptimizer.ts`) силён и считает листы по выделенным заказам ✅; не хватает: сумма ₽ в UI раскроя, печать заявки поставщику, связка раскрой→`purchase_orders`, сверка со складом, ветвление маршрута (сверловка/криволинейка/фацет/триплекс), трекинг подрядчиков «отправлено/вернулось».
- Google-таблица скачана: `scratchpad/mglass_2026.xlsx`. Помесячные листы 2024–2026, колонки заказа = № / Менеджер / Клиент / Дата запуска / суммы / стадии (Распечатан…Отгружен).

## Прошлый трек (P0 безопасность) — батч 1 закрыт
- `lib/apiAuth.ts` +`requireRole([...])`; гарды на 10 операций (payment, warehouse, b2b-seed, materials/from-supplier+transfer, mirror-lighting(+tabs), purchase-orders, procurement-routes, bot-toggle). «Без guard» 66→56. `scripts/audit-guards.mjs` — страж.

## Что сделано (эта сессия)
- Сквозной аудит 6 аналитиками + `docs/AUDIT_2026-06-30.md`, `docs/WORKING_RULES.md`, `scripts/audit-guards.mjs` (страж покрытия).
- **Батч 1 гардов (10 операций закрыто, «без guard» 66 → 56):**
  - `lib/apiAuth.ts` — добавлен helper `requireRole(allowed[])`.
  - `orders/[id]/payment` → requireRole(admin,ceo,manager,buyer).
  - `admin/warehouse` → requireOwner; `admin/b2b-seed` (массовый DELETE) → requireOwner.
  - `admin/materials/from-supplier`, `admin/materials/transfer`, `admin/mirror-lighting`, `admin/mirror-lighting/tabs`, `admin/purchase-orders`, `admin/procurement-routes` → requireRole(admin,ceo,buyer).
  - `admin/bot-toggle` → requireRole(admin,ceo,seo).
- Проверено: tsc в боевом коде 0 ошибок, vitest 23/23, страж подтверждает закрытие.
- Роли подобраны по `ROLE_ALLOWED` так, чтобы не выбить легитимных вызывающих.

## Следующий шаг
Батч 2 P0 (по приоритету):
1. Order-lifecycle service-role роуты по правильным ролям (производство легитимно зовёт стадии): `orders/[id]/{brigade,delivery,production-stages,rating,photos}`, `appointments`, `measurer`.
2. `ai/proposals/*` (draft/approve/reject) — гард по роли.
3. `ai/generate-task`, `ai/analyze-note` — добавить auth (сейчас вообще без неё).
4. Защитить доступ к AmoCRM-write роутам (`amo/calls/analyze` без auth) — НЕ удаляя запись (решение по записи за владельцем).
5. Проверить наличие `CRON_SECRET`/секрета Wazzup в проде → fail-closed для `wazzup/webhook`, `cron/process-queue|process-tasks`.

## Контекст
- Корень дыры: `lib/getRole.ts:230` — `/api/*` всегда `true`; вся защита API на самих роутах.
- Страж: `node scripts/audit-guards.mjs` (regex детектора знает requireOwner/requireAdmin/requireRole).
- Пре-существующее: 31 ошибка tsc только в `__tests__/calculators/mirror.test.ts` (зовёт calculateMirror с 4 арг вместо 3) — быстрый follow-up, чтобы typecheck стал зелёным гейтом.

## Открытые вопросы
1. AmoCRM-записи (`cron/process-queue`, `amo/calls/analyze`) — фича или убрать? (запись не трогаю до решения; доступ к роуту защищу)
2. RLS на `glass_price_matrix` — включать только после проверки чтения калькуляторов.
3. Подтвердить `CRON_SECRET`/секрет Wazzup в проде перед fail-closed.

## Контекст
- Корневой источник дыры: `lib/getRole.ts:230` — `/api/*` всегда `true`, middleware не гейтит API по роли.
- Роуты `orders/[id]/{status,brigade,delivery,production-stages}` легитимно зовут не-owner роли (производство) — гард подбирать по роли, не blanket requireOwner.

## Открытые вопросы (блокеры P0, решает владелец)
1. AmoCRM-записи (`cron/process-queue`, `amo/calls/analyze`) — намеренная фича или убрать? (нарушают read-only)
2. Включение RLS на `glass_price_matrix` — только после проверки, что чтение калькуляторов не сломается.
3. Cron/webhook fail-closed — подтвердить, что `CRON_SECRET` и секрет Wazzup заданы в проде.
