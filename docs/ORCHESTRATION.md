# Оркестрация вкладок M-Glass — живой реестр

> **Зачем.** Над M-Glass App параллельно работают несколько сессий Claude («вкладок»). 25.08 мы
> дважды начинали строить то, что уже готово (складской контур `/inventory`, ссылки-приглашения) —
> спасла только ручная проверка. Этот файл — единый источник правды «кто чем занят» + открытые
> межвкладочные передачи, чтобы не дублировать и не ломать чужое.
>
> **Правило.** Перед работой в чужом домене — свериться здесь. После значимого шага — дописать
> строку (карта или лог). Синхронизацию ведёт backbone-сессия (оркестратор).
>
> Смежные доки: `docs/CONFIGURATOR_COORDINATION.md` (граница Сайт+3D ↔ Прайс),
> `docs/WAREHOUSE_ARCHITECTURE.md` (склад), `docs/INVENTORY.md` (реализация склада).

## Карта доменов

| Домен | Маршруты / файлы | Ведёт (вкладка) | Статус |
|---|---|---|---|
| Прайс душевых (математика цены) | `lib/configurator/pricing.ts,kit.ts`, `admin/visualizer-pricing`, `api/configurator/quote` | shower-pricing | 🟢 идёт |
| B2B прайс / квоты | `calculator/b2b`, `b2b-quotes`, `api/admin/supplier-catalog` | b2b-price-system | 🟢 идёт |
| B2B корректировка итога просчёта | `b2b-orders` (наложение manualTotal) | b2b-quote-adjustment | 🟢 идёт |
| Клиентский кабинет / монетизация | клиентский кабинет B2C | client-cabinet | 🟢 идёт |
| Бухгалтерия | `/accounting`, `api/inventory/documents` | accounting-cabinet | 🟢 идёт |
| **Склад** | `/inventory`, `lib/inventory/**`, `api/inventory/**` | inventory (PR #237) | ⚠️ построен, **пуст** (0 позиций) |
| Кабинет партнёра (B2B) | `/partner`, `api/partner/**`, `b2b_clients` | партнёрская | 🟡 A11 (УПД/ЭДО) |
| 3D-конфигуратор / сайт | `components/configurator/**`, `app/configurator`, `app/embed/shower` | Сайт+3D | 🟢 идёт |
| Ограждения | калькулятор ограждений, `feat/railing-manager` | railing | 🟡 |
| Кабинет менеджера | `/manager`, `/manager-dashboard` | manager-cabinet | 🟡 периодически |
| **Backbone (ядро)** | ниже | **оркестратор** | 🟢 |

> Роли-сессии динамичны (появляются/закрываются). Колонка «Ведёт» — по последней сверке
> `ListAgents` + веток/PR; уточняется по ходу.

## Backbone — ведёт оркестратор (не отдаётся вкладкам)

- **Авторизация / роли / доступ:** `lib/getRole.ts` (`Role`, `ROLE_ALLOWED`, `canAccessRoute`),
  ссылки-приглашения (`user_setup_tokens`, `api/admin/invite-link`, `admin/users`). Роли `office`,
  `logist` заведены 25.08 (PR #272).
- **Сроки и B2B-поток:** дедлайн при запуске (`api/b2b-orders/[id]/launch-production`), «в срок %»
  (`admin/b2b-flow`, PR #256), карта загрузки цеха.
- **Кросс-аналитика / роль-старт / сводки собственнику и РОП.**
- **Роадмап A0→A50** (`docs/`… см. артефакт) — шаги, не привязанные к конкретной вкладке.

## ⛔ Блокеры уровня проекта (проверять до заявлений «задеплоено»)

- **Vercel режет деплои (25.08):** «Deployment rate limited — retry in 24 hours» на последних
  коммитах main. **Код в main ≠ код в проде** — боевой сайт остаётся на прошлой сборке.
  Сегодня НЕ писать «проверь на проде» про смерженное. Решение (апгрейд плана / ждать) — за владельцем.
  - Исключение: **изменения БД (миграции) живут сразу**, они применяются к базе напрямую и от
    деплоя не зависят. Так, фикс грантов `mark_order_stages` (#280) в проде **уже действует** —
    проверено по `pg_proc.proacl`: `authenticated | service_role`, anon нет.

## Открытые межвкладочные передачи (кто → что нужно сделать)

1. **→ Кабинет партнёра.** Инвайт «Клиент B2B» (PR #272) создаёт `users.role='partner'`, но **не
   привязывает** пользователя к юрлицу-заказчику (`b2b_clients.user_id`). Без привязки первый вход
   партнёра — пустой кабинет. Нужно: при выдаче инвайта партнёру связать/создать `b2b_clients`
   (онбординг — территория партнёрской вкладки, см. `docs/PARTNER_CABINET_ONBOARDING.md`).
   **Статус: открыто, исполнителя нет** (партнёрская вкладка не активна).

2. **→ Склад (/inventory) + backbone.** Шаг **С4**: резерв материала при запуске заказа.
   **Статус: в работе, разделён.** Склад делает чистую
   `reserveForOrder(docType, docId, items) → { reserved, shortages, alreadyReserved }` (идемпотентна,
   резерв ≠ расход); backbone вживляет вызов в `api/b2b-orders/[id]/launch-production` и делает
   «что забрать / что купить» на экране запуска. Контракт: **best-effort** — ошибка склада не
   блокирует запуск заказа (иначе при пустом контуре встало бы производство).
   ⚠️ Никому больше этот хук не брать — на нём уже двое.

3. ~~**Формат `stages.shipped` + атомарная запись.**~~ **ЗАКРЫТО** (#274): date-only в обоих
   писателях, новый RPC `mark_order_stages` (FOR UPDATE, точечный `jsonb_set`), убраны все 5
   блоб-записей `notes`. При приёмке найдена и закрыта дыра в грантах (#280).

## Данные, которых не хватает (нужно действие владельца, не код)

- **Склад пуст:** `inventory_items = 0`, `inventory_moves = 0`. Код наполнения готов
  (`/inventory` → «Из справочников» → выбрать всё → вкладка «Инвентаризация» → внести факт).
  Реальные остатки знает только цех — сидом не решается.
- **Оплаты B2B не оцифрованы:** в `payments` 103 записи, банковских поступлений нет. Ждём
  **пример файла выписки** от бухгалтера, чтобы подогнать маппинг колонок в импортёре.

## Правила, выработанные на практике (обязательны для всех вкладок)

Каждое родилось из конкретной ошибки или находки — не выдуманы, ссылки на случай приведены.

1. **Метрику строим на том, что люди уже делают, а не на том, что мы надеемся их заставить.**
   Кнопка «Взял в работу» существовала и собрала 0 нажатий за 2 месяца. «Время этапа» изначально
   стояло на этом сигнале — переставлено на интервал между двумя подряд «Готово» одного человека
   на одной станции: считается из того, что цех делает добровольно.

2. **Кнопка, которая создаёт человеку работу, использоваться не будет.**
   `problem` = 0 при 3670 задачах не разгильдяйство: отметка вынимала задачу из потока, и её же
   потом надо было закрывать на отдельной доске. Контрпример в той же системе: «нет материала»
   используется (83 заказа), потому что даёт результат — заказ уходит в закупку. Проверяй шаг
   вопросом «что человек с этого получит», а не «сколько это кликов».

3. **Признак без данных обязан молчать, а не кричать.**
   Долг перед отгрузкой зажигается только при частичной оплате (0 < оплачено < сумма); ноль
   платежей = «оплата не заведена», не «должник». Иначе на 5000 заказов без импорта выписки
   система кричала бы на всё подряд и её перестали бы читать.

4. **Процент по узкой выборке нельзя показывать без покрытия.**
   «В срок %» считался честно, но по 13% отгрузок (у остальных дата не сохранялась) — и «из 267
   заказов» читалось как «почти все». Рядом с процентом всегда доля покрытия.

5. **Если проверку физически нельзя сделать — назови её невыполнимой и перечисли, что проверено
   вместо неё.** Имитация проверки хуже её отсутствия: снимает вопрос, не отвечая на него.
   (verified-by-code-path вместо живого SELECT на пустом складе.)

6. **Автоматическая запись не должна выдавать план за факт.**
   Движения склада несут `origin`: `plan` (списано по резерву) против `fact` (человек подтвердил).
   Без пометки остаток снова стал бы фикцией — только красивой и незаметной.

7. **Побочный системный эффект пишем server-only функцией, а не расширением прав человека.**
   Списание при отметке «упаковано» идёт через `consumeForOrder` (`import 'server-only'`), а не
   добавлением менеджера в `requireInventoryWrite` — иначе он смог бы править остатки напрямую.
   Прецедент: `reserveForOrder` на `launch-production`.

8. **`notes.stages` — общая свалка производства, отгрузки и денег** (`shipped` 4375, `invoice_paid`
   90, производственные этапы). Трогать оттуда можно ТОЛЬКО ключи своего контура и только через
   явный белый список, никогда «снимем всё, что переоткрыли».

9. **Тестовые данные в боевой базе не создаём.** Они переживают задумку, и через месяц их не
   отличить от настоящих; инвентаризация владельца должна начинаться с нуля, а не с чужих чисел.

10. **Настройки прав и доступа не едут в продуктовых PR.** Права меняются решением владельца
    осознанно, а не «заодно». (Файл авто-разрешений на `git push`/`gh pr merge` проехал в main
    внутри продуктового PR и был вычищен отдельно.)

11. **Историю не выдумываем.** Дату отгрузки для 3788 заказов с голым `true` восстановить неоткуда;
    исполнителя для 137 отметок без `updated_by` — тоже. Пустое поле честнее выдуманного.

12. **Одинаковая метрика по разным категориям — признак того, что меряется не то.**
    Время этапа без отбрасывания «загрязнённых» интервалов давало ровные 19–20 минут на всех
    четырёх станциях. Число красивое и правдоподобное на вид — но одинаковая длительность у
    физически разных операций неправдоподобна. Пересчёт по человеку вместо станции дал 5.2 минуты,
    вчетверо меньше, и разрыв выдал артефакт. Сходство категорий надо доказывать, а не принимать.

13. **Пустая клетка называет причину, а строка без данных не исчезает из таблицы.**
    Станция, тихо выпавшая из отчёта, читается как «её нет», хотя она есть и именно она не
    измеряется. Отсутствие данных — это тоже факт, и его надо показывать.

14. **Считай важную метрику двумя независимыми способами.**
    TS-реализация сверена с независимым SQL на проде — сошлись до цифры. Дешевле любого ревью и
    ловит то, что ревью не поймает.

16. **Экран, построенный по факту действия, не показывает тех, кто действия не совершает —
    а это обычно и есть проблема.** «Кто что делал» строился по отметкам, поэтому работник без
    отметок в него физически не попадал: не «показывался нулём», а отсутствовал в списке.
    Проблема была невидима ровно там, где на неё смотрят. Строй список от ПОЛНОГО состава
    (все работники / все станции / все клиенты), а факт действия показывай как признак.

17. **Общее поле `notes` целиком не писать — и это правило состоит из двух половин.**
    Плоские ключи (`deadline_date`, `payment_status`, `drawing_url`) — через
    `patch_order_notes_shallow`. Вложенный `notes.stages` — ТОЛЬКО через `mark_order_stages`:
    shallow-патч мержит верхний уровень, значит `{ stages: {...} }` заменит весь объект и
    вернёт клоббер этапов, ради лечения которого RPC и появился. Без второй половины первая
    опасна. Удаление ключа shallow-патч не умеет — передавай явный `null`, предварительно
    проверив, что читатели смотрят на truthiness, а не на `hasOwnProperty`.

18. **Миграция сделана только тогда, когда видно, что она ПРИМЕНИЛАСЬ.**
    Уникальный индекс счетов лежал смерженным в репозитории и не мог примениться в принципе:
    `array_to_string` не IMMUTABLE, Postgres такое в индекс не берёт. Защита от гонки двух
    печатей существовала только в файле. Сегодня же ловили похожее с грантами RPC. Проверяй
    факт в базе (`pg_indexes`, `pg_proc.proacl`), а не факт мержа.

19. **Цифра, допускающая два прочтения, обязана назвать оба — если одно из них уже приводило
    к неверному выводу.** Очередь 645 на полировке читается как перегруз, а означает
    неотмеченную работу. На этом ошиблись и вкладка, и оркестратор, и вывод дошёл до владельца
    как факт. Подпись под цифрой — не украшение, а защита от воспроизведения ошибки у каждого,
    кто на неё смотрит.

20. **Не строим аналитику поверх нулевых данных.** За день трижды отклонены готовые к работе
    задачи: аналитика брака (0 записей), корзины возраста долга (0 счетов, 0 банковских
    платежей), экран апсейла (пустой премиум-тариф). Механика в каждом случае верная — не
    хватало внешнего события, которое кодом не приближается. Пустой экран не «наполнится сам»:
    он приучает не смотреть.

15. **Перед пушем подтягивай main.** Десять параллельных вкладок мержат быстрее, чем одна успевает
    ребейзить; окно между «чисто» и «снова разошлось» — минуты. Ребейзнул → пингани оркестратора
    одной строкой «#N чист», мерж идёт сразу. (Авто-мерж в репозитории запрещён настройкой —
    владельца просили включить.)

21. **Проверка по коду — это гипотеза, а не проверка.** За два дня одно и то же расхождение
    поймано трижды, каждый раз в новом виде: класс в коде против пикселей на экране
    (`py-1` формально «не меньше минимума», в реальности 28 px — и это самая нажимаемая
    кнопка цеха); счётчик на кнопке против того, что делает сервер («закрыть 5» закрывало 61);
    файл миграции против того, что применилось в базу. Общее правило: **проверяй результат,
    а не запись о результате**. Если проверен только код — так и говори, не выдавай за проверку.

22. **Экран за логином проверяется на стенде, а не входом под чужой учётной записью.**
    Временный раздел на вымышленных данных, в него монтируется НАСТОЯЩИЙ компонент, замеряется
    реальный рендер, стенд удаляется. Так проверено 375×812 в цехе: найдены кнопка 28 px и
    подтверждение, стоявшее в 8 px от соседней кнопки, которая закрывает заказ целиком.
    Вход под рабочим создал бы боевые отметки его именем.

## Лог оркестрации (новое — сверху)

| Дата | Кто | Что |
|---|---|---|
| 2026-08-26 | backbone | Цех: волна I + П6 (аудит 26 экранов, входы, слияние обзорных, регламент, этикетки со сквозным номером листа, состав цеха). Менеджер: счёт→запись, дебиторка из платежей, notes-гигиена. Закрыта дыра в CI (не проверялись типы — красный main заморозил прод). Опровергнут собственный вывод «узкое место — один человек»: четверо из шести не входят в приложение. |
| 2026-08-26 | backbone | Круг 3: производство П1–П3 (#303/#306/#311), склад резерв+списание+origin (#305/#307/#312/#313), покрытие «в срок %» (#310), здоровье данных (#308), утечки партнёру закрыты системно (#299/#302). Записаны 12 правил, выработанных на практике. Вкладки трижды поправили оркестратора по существу — все три раза приняты. |
| 2026-08-25 | backbone | Круг 2: A18 роль-старт (#279), фикс грантов mark_order_stages (#280), «в срок %» + оценка по нормативу (#283, 66% на 267 заказах). Принял #274/#278. Зафиксирован блокер деплоя Vercel. |
| 2026-08-25 | backbone | Завёл этот реестр. A2 «в срок %» (#256), роли-инвайты office/logist (#272). Обнаружил дубли: склад и инвайты уже были — строить не стал, передал гэпы владельцам. |
