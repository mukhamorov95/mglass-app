# Вкладка «Производство» — состояние сессии

> Хендофф вкладки `production-tab-analysis-739b51`. Отдельным файлом, а не в `SESSION.md`:
> над репо параллельно работают десять вкладок, и один общий файл означает либо конфликт на
> каждом ребейзе, либо молчаливую перезапись чужого хендоффа. Второе хуже — это потеря данных.
> ТЗ и маршрут — `docs/PRODUCTION_PLAN.md`, реестр вкладок — `docs/ORCHESTRATION.md`.

## Текущая задача
Маршрут П1…П20 (`docs/PRODUCTION_PLAN.md`, ТЗ backbone-оркестратора). Волна I «Достоверность цеха».

## Статус шагов

| Шаг | Что | Состояние |
|---|---|---|
| П1 | Исполнитель проставляется сам | принят оркестратором, PR #303 |
| П2 | «В работу» без трения | готов, ветка `claude/production-p2-frictionless-start`, ждёт мержа П1 |
| П3 | Брак как рабочий статус | следующий |
| П4 | Время этапа | переформулирован (см. ниже), не начат |
| П5 | Переделка ≠ новая работа | не начат |

## Что сделано

**П1 — исполнитель проставляется сам.** Миграция `20260901_production_task_executor.sql`
(ПРИМЕНЕНА): `completed_by`, `completed_by_name`, `problem_by`, `problem_resolved_by`, индекс
`pt_output_by_worker_idx` (с `auto_closed = false` прямо в индексе — каскад не выработка).
Догон истории из `notes.detail_stages.updated_by` поднял исполнителя у **1431 из 1683**
закрытых задач. Логика → `lib/production/executor.ts`. Закрыты ОБЕ точки записи:
`api/production-tasks/[id]` (очередь цеха) и `api/b2b-orders/[id]/sync-stages` (карточка
заказа и QR-экран `/p/o`).

**П2 — «В работу» без трения.** Миграция `20260901_production_task_started_via.sql`
(ПРИМЕНЕНА): `started_via` (button | open | scan) + CHECK, `started_by`, `started_by_name`,
индекс `pt_in_progress_by_worker_idx`. Логика → `lib/production/start.ts`.
`POST /api/production/start` — один вход и для автостарта, и для кнопки «Взял»; готовность
этапа проверяется на сервере, не по списку из браузера; идемпотентен. В `my-queue` раскрытие
карточки заказа = автостарт; кнопка «Взял» оставлена и ходит тем же входом.

## Ключевые решения (нарушать нельзя)

- **План и факт разведены.** `assigned_to` — ПЛАН, питает фильтр очереди
  (`assigned_to = я ИЛИ assigned_to пуст И станция моя`), меняет его ТОЛЬКО явное «Взял».
  `started_by` / `completed_by` — ФАКТ. Автостарт `assigned_to` не трогает: иначе раскрытие
  карточки уводило бы работу из общего пула станции и второй рабочий её не увидел бы.
- **Каскадным задачам** (`auto_closed = true`) исполнитель не приписывается — их никто не отмечал.
- **Отмена этапа** снимает `completed_by`, `started_by`, `started_via` — иначе человек попадёт
  в выработку за невыполненное.
- **На `problem` `assigned_to` не ставится** — после снятия андона задача уходит в `queued`,
  приватная привязка спрятала бы её от станции.
- **П4 не строится на `started_at`** (согласовано с оркестратором): автостарт завышает
  длительность на время, что карточка висела открытой. Основная мера — интервал между двумя
  подряд «Готово» одного человека на одной станции (считается из того, что дал П1);
  `started_at` только при `started_via = 'button'` как контрольная выборка.
- **П12 переформулирован** (согласовано): не раскладка задач между станциями — станций восемь,
  людей двое, — а сигнал «где очередь растёт быстрее, чем её разбирают».

## Что вскрылось в данных (замер 25.08.2026)

Резку закрывает Бекмурза (537 отметок), всю обработку после неё — полировку 243, закалку 213,
упаковку 294 и подмену на резке 129 — один Никита (885). Очередь: резка 134, полировка 648,
упаковка 642, закалка 494. Средний срок этапа после резки ~10 суток.
**Узкое место цеха — человек, а не приложение.** Записано в `brain/people/TEAM.md` и
`brain/lessons/README.md`.

## Долг, найденный по ходу (чинить в волне I)

`lib/productionOrderMirror.ts` пишет `notes` ЦЕЛИКОМ (`update({ notes: JSON.stringify(notes) })`)
— последняя выжившая блоб-запись: две одновременные отметки по одному заказу могут затереть
друг друга. В репо есть атомарный RPC `mark_order_stages`. Чинить до того, как на этот файл
повесится вызов списания склада (П19).

## Открытые вопросы

- Адилет (сверловка, оклад ~150 000 ₽/мес) в отметках почти не появляется — 4 задачи сверловки
  за два месяца закрыл Никита. Вопрос у владельца через оркестратора. От ответа зависят П12 и П17.
- 137 закрытых задач так и остались без исполнителя — закрыты путём, не писавшим `updated_by`.
  Не чиню: выдумывать исполнителя нельзя.

## Стыковки с другими вкладками

- **Склад (П19).** Точка вызова списания зафиксирована: переход флага `packaged` внутри
  `mirrorOrderStages` (упаковка — всегда последний этап, единственный без условия применимости).
  Контракт склада — `POST /api/inventory/consume { type:'b2b_order', id }`, best-effort,
  идемпотентно. Оговорка, о которой склад предупреждён: материал физически исчезает на резке,
  а не на упаковке, поэтому «остаток на руках» будет завышен примерно на цикл производства;
  доступный остаток при этом верен за счёт резерва при запуске.
- **Backbone.** `launch-production`, дедлайны, `lib/getRole.ts`, `admin/b2b-flow`,
  `production-app/load` — не моё.
