# Прайс душевых — рабочая вкладка (устав)

Отдельный контур работы: **прайсовая система душевых перегородок** (себестоимость → цена клиенту),
которую владелец настраивает по каждой модели М1…М12 на основе прайсов поставщиков.

Ветка: `claude/shower-pricing-system-725ae3`. База: `main` + влит `feat/shower-3d-configurator`
(коммит A2 — избранное/сравнение поставщиков), поэтому здесь самый свежий прайсовый код.

## Границы (чтобы вкладки не мешали друг другу)

| Владеет | Файлы |
|---|---|
| **Прайс душевых** (эта вкладка) | `lib/configurator/pricing.ts`, `lib/configurator/pricingStore.ts`, `app/admin/visualizer-pricing/**`, `app/admin/supplier-catalog/**`, `app/api/admin/supplier-catalog/**`, `app/api/admin/configurator-pricing/**`, `app/api/configurator/quote/**`, миграции `*_configurator_pricing*`, `*_supplier_price*` |
| **3D-модель / сайт** (вкладка конфигуратора) | `components/configurator/**` (сцена, 3D, клиентский UI), `lib/configurator/arrangement.ts`, `lib/configurator/catalog.ts`, `app/configurator/**`, `app/embed/shower/**`, `public/configurator/**` |
| Общее, только через договорённость | `lib/configurator/hardwareShapes.ts`, `hardwareSpecs.ts` (роли/формы фурнитуры — контракт между геометрией и прайсом) |

Правило простое: **геометрия даёт количества, прайс даёт деньги.** Контракт между ними —
`Quantities` (`sections`, `glassM2`, `profilePieces[]`, `tubePieces[]`, `hardware{}`, `roles{}`).
Пока контракт не меняется, обе вкладки правятся независимо и мержатся без конфликтов.

## Что уже есть в системе (факт на 2026-08-25)

- Справочник поставщиков `/admin/supplier-catalog`: **Ветро 3261** строк (−32%) + **АВ24 3677** (−25%) = 6938,
  `cost = розница × (1 − скидка)`. Импорт xlsx в браузере, ★ избранное, ⇄ сравнение поставщиков.
- Себестоимость визуализатора `/admin/visualizer-pricing` (тарифы `budget` / `premium`, таблица `configurator_pricing`):
  8 подгрупп по ролям — петли, ручки, ролики, профили, трубы, крепёж, заглушки, уплотнители.
- Движок: `computeQuantities(assembly)` → `computePrice(q, prices)`;
  `Цена изделия = Себестоимость / (1 − маржа 40% − налог 12%)` (канон компании, `calcFinancialModel`),
  сверху монтаж 6500 ₽ × секции + доставка 5000 ₽ + подъём.
- Профиль/труба считаются **хлыстами**: кусок → наименьший хлыст ≥ длины куска.
- Цена считается на сервере (`/api/configurator/quote`): публичному embed уходит только «от N ₽», себестоимость не утекает.

## Заполнено сейчас (tier budget, вёл zakupka@mglass.ru)

| Подгруппа | Позиции | Проблема |
|---|---|---|
| Петли | Balge-004/CP 2988 ₽, Европа FDP-115 1162 ₽ | — |
| Ручки | кноб FDR-30 292 ₽ | нет скобы для распашной, нет ручки-купе |
| **Ролики** | **пусто** | М8/М9/М10/М12 (раздвижные) не считаются — уходят в `missing` |
| Профили | FDPA-51, хлыст **2000** мм — 712 ₽ | один хлыст: кусок >2000 мм посчитается по цене 2000 → недосчёт |
| Трубы | FDT-352 30×10, хлыст **2000** мм — 2175 ₽ | то же |
| Крепёж | FDC-30 352 ₽, FDC-35 622 ₽, FDC-34 570 ₽ | три auto-позиции: кол-во по роли получает только первая |
| Заглушки | FDPA-500 158 ₽ | — |
| Уплотнители | Ч-образный 202 ₽, магнитный 1028 ₽, нижний 262 ₽ | то же — кол-во только первой |
| Тариф `premium` | строки в БД нет | — |

## Главный архитектурный пробел

Цены и состав сегодня **общие на тариф**, а не **на модель**. Движок берёт первую `auto`-позицию
подгруппы, поэтому М2 (распашная) и М10 (раздвижная) собираются одинаковым комплектом.
Владелец хочет настраивать прайс **под каждую модель** — значит нужен слой
**«Комплектация модели»**: для М1…М12 — какая именно позиция играет каждую роль и сколько её штук
(ролики на створку, крепёж на стойку, уплотнители по типу двери). Это следующая крупная задача вкладки.

## Журнал вкладки (2026-08-25)

В main влиты PR #226, #233, #234, #235, #236, #238, #239. Прод работает на этом коде.

- Движок прайса по моделям: библиотека позиций (цена вбивается один раз) + комплект модели
  (слоты ролей, ★ по умолчанию, порядок, правило количества), раскрой хлыстов с пропилом.
- Роли-варианты М1 (труба 90°/45°/стабилизатор/в потолок) — неактивные молчат, дырой не считаются.
- Погонные позиции считаются погонно: уплотнители 2.2 м, заглушка профиля по проёму двери;
  торцевая заглушка — по открытому торцу напольного профиля.
- П-профиль ОДИН на все стороны: куски стены/пола/верха идут в общий раскрой.
- Карточка позиции с сайта поставщика: ссылка (у АВ24 ищется по артикулу), фото, характеристики.
- Контракт с визуализатором: POST /api/configurator/options и /quote. GET снят — клиент на POST.

Открытый вопрос владельцу: «заглушка сверху» — над проёмом двери или верхний профиль целиком.
Не заведено в библиотеке: ролики, ручка-купе, штанга и крепление 45°, стабилизатор; тариф Премиум пуст.
