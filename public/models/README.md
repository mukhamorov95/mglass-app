# Каталожные фото моделей — один источник для «Расчёта» и для виджета на сайте.
# Имя файла = код модели латиницей в нижнем регистре: m1, m2, m4, m7, m8, m9, m10, m11, m12.
# Все кадры приведены к 760×950 (4:5 — пропорция карточки): изделие вписано целиком,
# поле добрано цветом соседнего края. Резать по карточке нельзя — у горизонтального
# М12 обрезало бы треть ширины вместе с крайними стойками.
# Заменяя кадр, сохраняй эту пропорцию, иначе карточки в сетке разъедутся.
