-- Ответ на обращение цеха (ПРИМЕНЕНА 28.08.2026).
--
-- Статус говорит «внедрено», но не говорит ЧТО именно сделали: рабочий видит
-- ярлык и не знает, попала ли в работу его мысль. Обращение без ответа обучает
-- не писать следующее — та же болезнь, что у кнопки, которая создаёт человеку
-- работу и не даёт результата.

ALTER TABLE public.production_ideas
  ADD COLUMN IF NOT EXISTS response text;

COMMENT ON COLUMN public.production_ideas.response IS
  'Что сделали по обращению — видит автор рядом со статусом';
