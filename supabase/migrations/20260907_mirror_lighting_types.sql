-- Зеркала 2.0: типы подсветки (аура / фронт / обе). Тип живёт в снимке расчёта,
-- схему deals/calculations не меняет. Здесь — только новые ВИДЫ позиций
-- справочника mirror_lighting_components: пескоструйная полоса (фронт) и сборка.
--
-- CHECK на component_type уже снят прежней миграцией, так что новые виды
-- добавляются строками. Заводим заготовки: выключены и с нулём — движок читает
-- только активные, значит занижения нет, а на экране видно, что заполнить.
--
-- Вкладки справочника для новых видов.
insert into public.mirror_lighting_tabs (value, label, sort_order) values
  ('sandblast', 'Пескоструй', 160),
  ('assembly',  'Сборка',     170)
on conflict do nothing;

-- Заготовки. Цену и название владелец впишет из своей практики (пескоструй —
-- своя работа, сборка — труд), затем включит.
insert into public.mirror_lighting_components
  (component_type, name, description, unit, cost_price, active, sort_order) values
  ('sandblast', 'Пескоструйная полоса — заполнить', 'Своя работа. Цена за пог.м полосы, затем включить', 'пог.м', 0, false, 10),
  ('assembly',  'Сборка подсветки — заполнить',     'Наклейка ленты, пайка, установка выключателя. У «обеих» считается ×2', 'шт', 0, false, 10)
on conflict do nothing;
